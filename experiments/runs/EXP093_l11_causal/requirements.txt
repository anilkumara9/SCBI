# EXP092 execution-bundle requirements (CPU-only, $0).
#
# Same pins as EXP091 (the LOG-331 deviation — accelerate missing — recurred
# at EXP090's launch and killed the run before extraction; the bundle MUST
# pin accelerate explicitly since transformers>=5 requires it whenever
# device_map is passed to from_pretrained).
numpy>=1.26
torch>=2.0  # CPU wheel: pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers>=4.40
safetensors>=0.4
accelerate>=1.0.0
