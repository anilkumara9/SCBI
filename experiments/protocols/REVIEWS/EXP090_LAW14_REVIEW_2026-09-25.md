# EXP090 — Independent Law #14 Protocol Review: SIGN-WITH-FIXES

**Reviewer:** Independent Law #14 Reviewer (reports DIRECTLY to the founder; verdict binding, not overridable by the CEO).
**Date:** 2026-09-25. **Logged:** LOG-348.
**Target:** `experiments/protocols/EXP090_CLM8B_ADAPTATION_V2_PREREG_DRAFT.md` (unsigned DRAFT; 295 lines).
**Parent:** `experiments/protocols/EXP089_CLM8B_ADAPTATION_PREREG_SIGNED.md` (immutable; digest `87f2c47b…4c24cb` — verified untouched, not re-read for content beyond diffing).
**Prior ruling:** LOG-345 (EXP089 §6 byte-reproduction contract CONFIRMED UNSATISFIABLE).

---

## Verdict: SIGN-WITH-FIXES

The draft's core design survives adversarial review. One precision fix (F1, record-accuracy in the §0 disclosure) and one wording fix (F2) are required before signing. Neither touches endpoint, verdict, cost, or mapping. The §0 deviation is **ACCEPTED** — restructure is **NOT** required (ruling in §2 below).

---

## 1. Verification of the drafter's unsatisfiability finding — CONFIRMED

I reproduced the drafter's forensics independently (read-only, my own executions):

- **Archived records lack prompts and foils.** `exp077_instance_records.json` (60 records; SHA-256 `47281cd3…0585`, character-identical to the §4/LOG-345 records) carries exactly the keys `[correct, ent, item, rescue_indicators, typ]`. No `prompt` key, no foil/`C` key in any record. An executor instructed to "read the 60 records" cannot produce the `(prompt, A, C)` items §6 requires. The CEO's literal brief was genuinely unsatisfiable.
- **The `(ent, typ)` multisets differ.** My Counter comparison (my own run of `benchmark_exp089.build_benchmark()` vs the archive): archive is uniformly grouped — Mars/Venus/Jupiter/Saturn/Mercury ×6, Iron/Gold/Silver/Bronze/Steel ×6; the repaired builder yields Mars×21, Iron×21, Venus×7, Gold×7, Jupiter×2, Silver×2. Positional match: **12/60**, first mismatch index 1 — reproducing LOG-345 exactly. No deterministic matching rule can recover executable items from the archive.
- The drafter was right to refuse the literal brief: drafting it would have registered a second unsatisfiable contract. This refusal is commended, not penalized.

**Correction to the record:** the draft's §0 says the archive is "Jupiter/Saturn-heavy." It is not — it is uniformly grouped (every entity ×6), as shown above. See F1.

---

## 2. RULING on the §0 deviation — SUBSTITUTION ACCEPTED; RESTRUCTURE NOT REQUIRED

The draft substitutes: bench = deterministic output of the verbatim-ported repaired EXP077 builder (`benchmark_exp089.build_benchmark()`), with the archive pinned as provenance via hash assert (not as item source), plus caveat 6 fencing any "exactly the bench EXP077 ran" claim.

**Is the substituted contract satisfiable?** YES — by construction. I executed `build_benchmark()` myself: 60 items, deterministic (pure Python over fixed module constants), G3 30/30 on the builder's own output verified at LOG-345. The executor runs the builder; no external reproduction requirement remains. The struck guard was the only unsatisfiable element.

**Is it non-vacuous?** YES. The registered bar (≥38/60, P=0.0259469 — recomputed exactly this session; P(X≥37)=0.0462, so the bar is sharp), the §7.1 TOTAL decision tree, and the G1–G4 guards are unchanged from EXP089 and were already adversarially attacked at LOG-336 (recency/primacy/edge-salience) and held.

**Does caveat 6 adequately fence the claim?** YES. I attacked this directly: the verdicts' licenses do not depend on EXP077 item-order identity. KILL ("frozen Pythia-410m/layer-20 geometry carries no exploitable zero-shot decision signal") and CONTINUE ("frozen geometry carries task-relevant signal") are claims about the *geometry on a fixed registered bench*, and the bench is fixed, registered, and reconstructible. The §7.1 positional diagnostic is bench-internal (phrasing splits are construction properties, i<8/i<7). The one claim that *would* require item-order identity — "this is exactly the bench EXP077 ran" — is precisely what caveat 6 declares UNLICENSED. The fence covers exactly the gap.

**Why not restructure as a fresh bench?** Restructure would sever the Law #9 reuse justification and the archive provenance pin while producing *literally the same 60 items under a new name*. The lineage claims that do real work are all true and verified: same construction family (vocab lists, index arrays, prompt templates — verbatim port verified at LOG-345), same frozen weights (G2 hash-matched to EXP077's own hashes), same layer-20 site, same entity vocabulary family. Item *order* was never load-bearing for the free-lunch question. Requiring restructure would be process theater. **Ruling: accept the substitution.**

**Binding note (not a fix):** caveat 6 is LOAD-BEARING. It must survive signing verbatim; any future write-up, bundle note, or report claiming EXP090 ran "the EXP077 bench" is unlicensed.

---

## 3. Carry-over check — everything else identical, verified by diff

Section-by-section normalized diff of the draft against the signed EXP089 protocol:
- **Identical:** §1 (precise question), §3 (Δθ=0 scope fence), §5 (Law #7), §9 (budget).
- **Benign deltas only:** §2 (+ succession history), §4 (reorganized; all four artifact hashes character-identical to parent), §7 (wording of the exactness note only; bar unchanged), §8 (RUN-INVALID list swaps the struck regeneration-mismatch for the archive-hash mismatch; refusal gains the digest-guard clause already present in the EXP089 bundle), §10 (one word), §11 (+ caveat 6), §12 (launch-chain position).
- §6 is the single declared design change. No silent hypothesis shift: the precise question and all verdict semantics are byte-identical.

---

## 4. Required fixes

- **F1 (precision, record-accuracy):** §0's forensics sentence is factually imprecise — the archive is NOT "Jupiter/Saturn-heavy." Replace with the verified multisets: archive uniformly grouped (Mars/Venus/Jupiter/Saturn/Mercury ×6 planet; Iron/Gold/Silver/Bronze/Steel ×6 element); repaired builder Mars×21, Iron×21, Venus×7, Gold×7, Jupiter×2, Silver×2 (Counter comparison, this session). The conclusion ("multisets differ; no deterministic matching rule can recover executable items") is unchanged. The §0 disclosure is the evidentiary record for refusing the brief — in an immutable signed protocol it must be exactly right.
- **F2 (precision, wording):** §6 — "the verbatim port guarantees the bench is the registered construction" → "the LOG-345-verified verbatim port implements the registered construction (same vocab lists, index arrays, prompt templates; only addition is the `phrasing` provenance field)." "Guarantees" overclaims; the verification is the evidence.

Neither fix changes endpoint, verdict, cost, or mapping — **no new experiment number triggered by the fixes.** Applying F1–F2 to the unsigned draft is a documented revision.

---

## 5. Attack log (what I tried to break)

- Re-ran the builder/archival comparison myself (12/60, index-1 first mismatch) — the provenance break is real, fourth reproduction.
- Attempted a fifth route to satisfiability (hand-matching archive order to builder items): impossible — prompts and foils are absent from the records, and multisets differ. No route was missed by the drafter.
- Checked the bar's sharpness independently (P(X≥38)=0.0259469; 37/60 would not reject at α≈0.026).
- Checked for silent scope creep in the substitution: the draft's §3 fence is byte-identical to the parent's; the succession banner correctly marks EXP089 SUPERSEDED-BY-EXP090 *for execution only* (Law #8: EXP089 stays on record, not deleted).
- Checked the §8 digest-guard addition: it registers behavior already present and tested in the EXP089 bundle — a tightening, not a design change.

---

**License state:** the draft licenses nothing. On F1–F2 application (verified by diff, not attestation), the draft is clear for the CEO's signing decision. $0 CPU; no weights, artifacts, or signed files were modified by this review (all reads read-only).
