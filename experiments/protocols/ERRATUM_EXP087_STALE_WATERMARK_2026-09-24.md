# ERRATUM — Stale draft-era wording in EXP087 signed protocol

**Adjudication:** LOG-329 (Independent Scientific Mentor, binding; 2026-09-24).
**CEO implementation:** Nova, 2026-09-24. Append-only; the signed file is NOT edited.

## File identity

- File: `experiments/protocols/EXP087_TRANSLATION_SIGNATURE_PREREG_SIGNED.md`
- SHA-256 at adjudication time: `32c27415b20fa0d07adb4fffb61127859c5e726fbfecff642a89e91fc1358d23`
- Signed at: LOG-324 (with explicit R5 resolution: option (a) adopted — if μ̂
  falls outside [0.649, 0.849], verdict is RUN-INVALID, withheld pending rerun).

## Why this erratum exists

The signed file was generated by prepending a signed header to a draft-derived
body. Retained pre-signing language contradicts the binding layer in six
passages. Silent editing is forbidden (LOG-329: immutability rule; Law #14
review-to-artifact link; Law #12). This erratum is the remedy instrument.

## Precedence rule (program-wide, LOG-329 Order 2)

The signed header block + embedded at-signing resolutions = the BINDING LAYER.
The retained draft body = adopted design text. On any conflict, the binding
layer governs. LOG-329 is the recorded authority.

## Passage catalogue

| ID | Severity | Location | Retained wording (summary) | Binding reading |
|----|----------|----------|----------------------------|-----------------|
| E87-1 | (b) misleading | lines 14–15 | "STATUS: DRAFT — NOT SIGNED." | FALSE. Line 3 records SIGNED at LOG-324. |
| E87-2 | (b) misleading | lines 15, 23–24 | "No experiment number is minted… the number mints only at signing." | FALSE. The number EXP087 is minted; the protocol was signed at LOG-324. |
| E87-3 | (b) misleading | line 24 | "Do not execute." | Overstates the restriction. Binding license at signing: CPU execution-bundle construction (bundle review + CEO clearance required before any GPU). |
| E87-4 | (b) HIGHEST SEVERITY | line 32 | "(table left as LOG-306 signed it)" | FACTUALLY FALSE post-signing. The §2 R5 row WAS re-mapped at signing to RUN-INVALID (CEO adopted reviewer option (a)). A reader trusting the blockquote applies the struck CONTINUE mapping — the exact misreading the at-signing decision was designed to prevent. The binding layer (at-signing resolution) governs; the blockquote is superseded. |
| E87-5 | (c) ambiguity | §8 items 3–5 (lines 321–325) | OPEN design items unresolved at signing. | RESOLVED by LOG-329 Order 3: delegated to the bundle-build lane with recorded rationale + independent bundle-review verification. Any resolution changing a registered endpoint, verdict, cost, or mapping → new experiment number, never an edit. The CEO MUST write this delegation into the bundle-build mandate before build starts. |
| E87-6 | (a) cosmetic | line 222 | Draft-era sequencing narrative. | Cosmetic. Retained as-is. |

## CEO standing note (Order 3)

Before the EXP087 bundle build starts, the build mandate MUST include: "§8
items 3–5 are delegated to the bundle-build lane; record rationale for each
resolution; all resolutions subject to independent bundle review; any change to
a registered endpoint/verdict/cost/mapping requires a new experiment number."

## Binding determinations

- No stale passage alters a registered endpoint, verdict rule, cost, or decision
  mapping in the binding layer (E87-4 is contradicted by the unambiguous binding
  layer). Re-signing is NOT ordered (LOG-329).
- Recourse for disputes: a new mentor adjudication, never a silent edit
  (LOG-329 Order 6).
