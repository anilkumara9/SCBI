# SCBI Candidate Backlog — the live innovation queue

*Standing workstream per LOG-219. Reviewed at the Monday session; cut at
the Friday cull if missing kill criteria or a costed pilot. Rule: every
null or kill produces ≥1 new falsifiable candidate — logged before the
workstream closes ("what did this death teach us").*

| ID | Candidate | Question it answers | Kill criterion | Cheapest falsifying experiment | Pilot cost | Belief it could change | Status |
|---|---|---|---|---|---|---|---|
| S3-1 | ARP — Attention-Response Profile verifier | Can a target-free causal fingerprint separate rescue from readout tilt? | $0 archived AUC gate fails (pre-registered) | Archived-record verifier analysis | $0 archived (+0.008 T4-h live) | That target-free causal verification is impossible on archived data | BACKLOG — K1 verdict landed (LOG-213/217); **archived Stage-0 gate KILLED as infeasible LOG-243** (no per-layer activation/attention snapshots exist in any archive — schema walk over 8 artifacts + exp077_vectors.pt, which holds only L20 direction vectors). **PIVOT:** Stage-0b live pilot QUEUED — 240 passes (60×4) ≈ 0.003 T4-h; needs Law-#14-signed plan + CEO GPU clearance; re-frame separation target (rescue vs relational-readout baseline — tilt framing is dead post-K1/EXP082). |
| S3-2 | LOM — Library of Operators, per-instance mixing | Can operators transfer across tasks via per-instance mixing? | Pilot gate fails (pre-registered) | Small pilot, operator retrieval | 0.006 T4-h | That representation discovery must be per-task | QUEUED — gate after K1/K2 sequence |
| S3-3 | DUG — Doubt-gated escalation (layer-disagreement) | Does layer disagreement predict when computation is insufficient? | $0 archived AUC gate fails (pre-registered) | Archived layer-disagreement analysis | $0 archived (+0.005 T4-h live) | That models can't self-diagnose insufficient computation | BACKLOG — K1 verdict landed (LOG-213/217); **archived Stage-0 gate KILLED as infeasible LOG-243** (no mid-layer residual snapshots in any archive — D_layer uncomputable; same schema walk as S3-1). **PIVOT:** Stage-0b live pilot QUEUED — 60 doubt-probe passes ≈ 0.005 T4-h, pilot gate AUC > 0.6 before further spend; needs Law-#14-signed plan + CEO GPU clearance. |
| S3-4 | SAH — Stability-hysteresis α control | Does flip-stability separate bridge from B_wrong on an α grid? | Stage-0 gate: bridge-vs-B_wrong stable-flip separation, p ≥ 0.1 (no separation) | Live 360-pass α grid (Stage-0 IS the pilot) | 0.005 T4-h | That static-geometry interventions are decision-flat | REDUCED — cone-arm-only CPU characterization done (LOG-216); full gate needs Law-#14-signed plan + CEO GPU clearance |
| S3-5 | ACG — Adaptive computational graphs | Can conditional micro-intervention DAGs compose? | Pilot gate (TBD) | TBD — needs Law-#15 answers | TBD | TBD | IDEA — needs Law-15 four answers + belief-change statement before it can queue |
| S3-6 | AAR — Adversarial attack-survival selection | Can falsification serve as the verifier? | Pilot gate (TBD) | TBD — needs Law-#15 answers | TBD | TBD | IDEA — needs Law-15 four answers + belief-change statement before it can queue |
| S3-7 | Spoiler-suppression (third-token elimination) | On bridge-rescued items, was the baseline prediction the foil or a third token? | Archived labels absent → Underdetermined (stays queued); labels present + baseline=foil on majority of rescued items → H killed; third-token majority → H survives, earns pre-registration | $0 archived-record inspection: `exp077_instance_records.json` schema for predicted-token labels on C1 vs C8_bridge; tabulate baseline prediction on rescued items | $0 CPU | That the bridge's rescue is a directional readout effect at all — it may be competitive elimination, reframing the tilt program | QUEUED — LOG-221 (2026-09-23): **Underdetermined** — no predicted-token labels in any archive (EXP077: 14/60 rescued identified but token-ids absent; EXP066: margin scalars encode no identity; EXP065: aggregate-only). INSTRUMENTATION SPEC for any future run: per-item per-arm top-5 token strings+ids+logits, baseline top-1 competitor identity, margin decomposition, Δθ=0 hashes (Law #7 audit applies). Future pre-registration must carry a CAUSAL endpoint (competitor-logit-drop: bridge vs random-rotation control on rescued items), not the tabulation alone. |
| S3-8 | Downstream-amplification test (does the (t−f) direction need depth?) | Is the rescue fully explained by the direct L1 readout shift, or does it require downstream layers? | Per-item residual \|archived end-to-end margin shift − L1-predicted shift\| ≈ 0 on rescued items → downstream-amplification dead (K2's routing premise dissolves); large residuals → downstream transformation real, K2's GPU minutes justified | $0 CPU: per-item L1-predicted vs archived end-to-end margin-shift decomposition on EXP065/066 rescued items (archived logit shifts exist) | $0 CPU | Whether ANY upstream mechanism is left to hunt — decides if K2 is worth its GPU minutes | COMPLETE — LOG-225 (2026-09-23): **Supported (EXP066)** — L1 explains only ~57%; mean residual +0.3225 (60/60 positive), ×1.8 downstream gain (g mean 1.797, rescued n=8). K2 recommendation: PROCEED as attribution pilot. EXP065 Underdetermined; EXP077 not decomposable. Numbering: dispatched as LOG-222, renumbered LOG-225 after collision with the Benchmark Analyst hire. |
| S3-9 | Gain tomography g(α) — is the ×1.8 downstream gain a fixed linear property or strength-dependent routing? | α-grid injections over the 8 rescued items; g varying >20% kills the linear-amplifier story; flat g licenses linear-gain modeling | $0 candidate review first — 80 passes (~0.0005 T4-h) proposed; may fold into K2's battery rather than run standalone | Pending — candidate only, not licensed | $0 | Whether the downstream amplifier is linear or routing-like — the cheapest quantitative question the ×1.8 gain left open | QUEUED — Law-#15 four answers + belief-change + steelman recorded LOG-244 (2026-09-23): promotable; pilot still needs pre-registered plan + Law-#14 SIGN + CEO GPU clearance. Recommendation: FOLD into K2's battery (≈24–32 add-on passes on the 8 rescued items, zero launch overhead); fold-vs-standalone decided at K2 review/CEO clearance. |

## Deaths that owe us candidates (LOG-219 rule)

| Death | What it taught | Candidate owed | Status |
|---|---|---|---|
| EXP065/066 Procrustes claims retracted | Rank-≤2 unembedding fit ≠ full-residual geometry | S3-4's per-arm (not pooled) analysis discipline | PAID (LOG-216) |
| G1 killed QK-null-space mechanism | Mechanism must be readout-side or unknown | EXP082 foil-suppression tilt (LOG-214) | PAID (executing, LOG-217) |
| EXP077 static geometry decision-flat | Transferable direction lives output-side | K2 routing/bypass pilot (gated on K1) | PENDING K1 |
| K1 verdict (2026-09-23): readout-tilt EXONERATED | Bridge is a relational (t−f) direction, not a target boost; both directional tilts dead; rescue must be relational readout effect or downstream transformation | S3-8 downstream-amplification test (does the (t−f) direction need depth?) | PAID (LOG-222 dispatched same cycle) |
| EXP082 verdict (2026-09-23): foil-suppression EXONERATED | Bridge is neither target- nor foil-directed on any item, yet rescues +10pp — static directional-readout story dead; rescue must come from elsewhere | S3-7 spoiler-suppression (third-token elimination) | PAID (LOG-221 dispatched same cycle) |
