# Cluster C — Futures (tracks 6 systems, 9 benchmark scientist, 10 architecture inventor, 11 scaling researcher)

**Dispatch:** LOG-140 (A–J synthesis commission). **Role:** track-cluster lead, Cluster C.
**Sections owned:** E (track 10 lead), F, G, H, J (with Cluster A's track-5 I-input — see §0.3).
**Status:** analysis only. No GPU used. No signed protocol modified. No numbers invented.

## 0. Reading record, labels, and dependencies

**Read before writing** (knowledge protocol §1; naming what was read):
1. `research/synthesis/SYNTHESIS_SPEC.md` — binding brief, honest evidence base (§2 FACTS), P1–P4, boxed standard, verdict standard.
2. `research/TEAM_KNOWLEDGE_PROTOCOL.md` — §1 reading list, challenge+idea obligation, double-labeling, verdict standard.
3. `AGENTS.md` (repo) — 14 laws, 10-label standard (§5).
4. `research/EXPERT_TRACKS.md` — track 6/9/11 charters, double-label mapping (FACT→[FACT]/[THEOREM]/[OBSERVATION]; INFERENCE→[INTERPRETATION]/[PROPOSITION]; HYPOTHESIS→[HYPOTHESIS]; SPECULATION→[CONJECTURE]/[OPEN]).
5. `research/CHATGPT_MENTORSHIP_DIRECTIVE.md` — canonical objective (θ_after=θ_before; temporary state (B_t,z_t,C_t,M_t,…) evolves), P2 license to propose a successor resembling nothing of SCPM, P3 kill-experiment gate, three evidentiary levels.
6. `research/innovation/SPRINT_2026-09-23.md` — EXP069–076 + N1–N7 proposals (corpus-search per protocol §1.C: none of my six candidates duplicates a sprint idea; deltas stated per candidate).
7. `theory/LOOP_SPEC_DRAFT.md` — the G/E/S/T loop (the incumbent mechanism family; E-validity still [CONJECTURE]).
8. `research/MATH_STANDARDS_CHARTER.md` — M1–M8 (all cost numbers below tagged [ILLUSTRATIVE] with derivation shown per M3; decision trees given exhaustive partitions per M5.2).

**Label convention used below:** every load-bearing claim carries `CANONICAL / 10-label`,
e.g. `[SPECULATION/CONJECTURE]`. Unlabeled sentences are scene-setting, not claims.

**0.1 Evidentiary levels.** Every result/claim states its level: L1 "can improve inference"
≠ L2 "changes the computational strategy" ≠ L3 "creates qualitatively new capability."
No silent crossing anywhere below.

**0.2 P2 posture.** SCPM/G-E/S/T is treated as one hypothesis family, possibly a dead end.
All six candidates are licensed successors; resemblance to SCPM is neither required nor
sought. [INFERENCE/INTERPRETATION]

**0.3 Dependency on Cluster A.** Track 5 (superintelligence/AGI architecture) owns
deliverable I (operational definition of "superintelligent augmentation"). Cluster A runs
in parallel; its I-input was not available to this cluster. §J below therefore uses a
**provisional operational definition, labeled [SPECULATION/CONJECTURE]**, and the
synthesizer MUST reconcile it with Cluster A's I before program adoption. Nothing in §J
is licensed until that reconciliation.

---

# E. Six candidate architectures for inference-time cognitive augmentation (track 10 lead)

All preserve Δθ=0 by construction (no optimizer, no gradient-applied parameter update;
argument per candidate). All target the organizing question at the mechanism level (P1):
*qualitatively new computation*, not benchmark scores. Cost sheets use the base unit
**1F** = one forward pass of the frozen backbone on one instance (FLOPs), **ℓ** = its wall
latency, **A** = its activation memory (weights W shared, already loaded). All derived
numbers are **[ILLUSTRATIVE]** (math charter M3.1) with the derivation shown; the regime
(pythia-410m-class, single T4) is stated where it matters.

**H1–H5 mapping:** C1→H1 (dynamic representation control), C2→H2 (ephemeral computational
workspace), C3→H3 (test-time program synthesis), C4→H4 (adaptive cognitive routing),
C5/C6→H5 (not-yet-named; two distinct inventions — the synthesis may keep one or both).

---

## C1 — Closed-Loop Latent Control (CLLC)

**Mechanism.** [HYPOTHESIS/HYPOTHESIS] Treat the residual stream as a dynamical plant and
steer it with *feedback*, not open-loop injection. Two frozen roles: a **plant** forward
pass generates the trajectory; an **observer** role (same weights, different prompt-role)
reads mid-layer residuals and a **controller** computes a correction
u_t = −K·(h_t − h\*) driving activations toward a reference state h\* (the setpoint =
the output-bridge direction's induced state, or the mean correct-answer state — both
pre-registered, label-informed only at setpoint-definition time, never per test item).
Gains K come from layer-wise Jacobians (A-LQR lineage, sprint N2) computed once per
task and cached. This is verifier/controller separation made literal: the controller
never generates content; the plant never evaluates itself. **Delta beyond the seed:**
the directive's "verifier/controller separation" is a slogan; CLLC is a control law
with a *tracking-error bound* — the only candidate offering a guarantee-shaped claim
rather than a search-luck claim — and the observer/controller/plant split is a
three-role hierarchy, not A-LQR's single loop.

**Δθ=0 argument.** [FACT/PROPOSITION] Jacobians are computed by autograd but no parameter
is updated (no optimizer step; gradients wrt activations only, parameters under
no-grad for the update path). By the loop spec §3 proof pattern: every mutation targets
activations h_t or the temporary gain cache (task-level, discarded); θ_t = θ_0 ∀t.

**Closest prior art / novelty note.** [INFERENCE/INTERPRETATION] A-LQR (Skifstad, Yang &
Chou, Apr 2026) is the direct prior: feedback control of activations. CLLC's novelty bid
is the observer–controller–plant separation with cached gains and a pre-registered
error bound evaluated on a *relational* task (A-LQR's bounds were shown on
toxicity/truthfulness-style steering). Honest tier: N1 (known combination) until the
bound holds on relational reasoning — then N2-adjacent on the *formulation*, with
operator credit remaining A-LQR's.

**Systems cost sheet (track 6).**
- Per item (uncached): 1F (plant) + 1F (observer) + Jacobian-vector products ≈ 2–4F
  [ILLUSTRATIVE; scales with #controlled layers] → **≈ 4–6F**, energy ≈ 4–6×.
- Per item (gains cached per task): **≈ 2F**, energy ≈ 2×.
- Latency: ≈ 2 sequential forwards + JVP time (observer must read plant residuals).
- Memory: 2 concurrent activation buffers ≈ 2A (weights shared).
- **What the extra forwards buy:** the only candidate whose claim is a *bound*, not a
  search outcome — if the tracking-error bound holds, steering becomes certifiable
  per item. Falsifier of the cost claim (pre-registered): Jacobian conditioning
  κ > κ_max → no control authority → the extra FLOPs provably buy nothing; run halts.

> **Box 1 — unique support:** Measured setpoint-tracking error stays within the
> pre-registered bound's order of magnitude on the relational task AND CLLC rescues
> items (ΔM>0, p<0.05) where open-loop injection of the *same final correction*
> (no feedback) does not. No competitor predicts bound-holding feedback to beat
> its own open-loop ablation — C2/C3/C6 have no feedback; C4 routes but does not
> control; C5 debates but does not track.
>
> **Box 2 — kill:** The A-LQR "faithful-but-inert" cell (sprint N2): ΔM_CLLC = 0
> while tracking-error bounds hold, OR bounds violated by >1 order of magnitude
> with ΔM = 0. Either way the controller formulation is withdrawn for relational
> reasoning — feedback control is not the missing mechanism. [HYPOTHESIS/HYPOTHESIS]

**Evidentiary target:** L2 (changes the computational strategy: open-loop → closed-loop)
as the claim; L1 gains alone do not license C1.

---

## C2 — Ephemeral Latent Memory (ELM)

**Mechanism.** [HYPOTHESIS/HYPOTHESIS] Give the frozen model a runtime RAM it does not
have: a fixed scratch buffer M_t of S slots (e.g. S=128, d=1024 → ~0.5MB
[ILLUSTRATIVE]) living *outside* θ, read/written across T sequential forward passes
via model-generated keys. Pass t: the model emits (key_t, write_t) alongside its
ordinary output; a fixed addressing operator (no learned parameters — dot-product
attention over slots) routes write_t into M_t and returns read_t = attend(M_t, key_{t+1})
as an additional input to pass t+1. θ_after = θ_before; only M_t evolves. This is
**latent workspace construction** made addressable: unlike chain-of-thought (text
tokens, no algebra), slots support compositional operators the interpreter defines
(e.g. slot-add for evidence accumulation, slot-compare for consistency). **Delta beyond
the seed:** the directive lists "ephemeral memory" as a noun; ELM specifies the
write/read addressing protocol, the cross-pass information contract (what must be
banked vs recomputed), and the diagnostic that the memory is actually used
(read-attention non-uniformity) — without which "memory" is a metaphor (math charter M7).

**Δθ=0 argument.** [FACT/PROPOSITION] M_t is a runtime buffer (like a KV-cache, which
already exists at inference); writes are activation copies, never gradient updates.
θ untouched by construction.

**Closest prior art / novelty note.** [INFERENCE/INTERPRETATION] DNC/NTM (Graves et al.)
are the priors — but those *train* the controller. ELM's bid: training-free addressing
with a frozen controller, i.e. memory as a *protocol* rather than a learned skill.
Honest tier: N1 until the read-attention diagnostic shows the model actually uses
slots non-trivially across passes.

**Systems cost sheet (track 6).**
- Per item: T passes × (1F + slot-attention O(S·d) ≈ 0.01F [ILLUSTRATIVE]) → **≈ T·F**,
  T ∈ {3..8} pre-registered. Energy ≈ T×.
- Latency: T sequential passes (the binding cost).
- Memory: +S·d floats ≈ 0.5MB [ILLUSTRATIVE] — negligible; weights unchanged.
- **What the extra forwards buy:** the only candidate with *accumulating* state —
  information persists and composes across passes instead of being recomputed
  (multi-hop evidence banking: pass 1 banks hop-1 facts, pass 2 reads them).
  Falsifier of the cost claim (pre-registered): read-attention mass ≈ uniform over
  slots (model ignores the buffer) → T·F buys nothing; halt.

> **Box 1 — unique support:** Ablating the buffer (T=1, no carryover) at matched
> total FLOPs removes ≥50% of the gain (pre-registered), AND read-attention over
> slots is significantly non-uniform (p<0.05 vs uniform) AND banked content is
> causally implicated (corrupting a banked slot flips the decision). No competitor
> has cross-pass accumulating state — C1/C4/C5/C6 recompute every pass; C3's
> "memory" is the synthesized program text, not a latent buffer.
>
> **Box 2 — kill:** Read-attention ≈ uniform (memory unused) with ΔM = 0, OR
> buffer-ablation at matched FLOPs leaves the gain unchanged (the gain was the
> extra passes, not the memory). Then ELM is "T forward passes with extra steps" —
> withdrawn. [HYPOTHESIS/HYPOTHESIS]

**Evidentiary target:** L2 (new strategy: accumulate-then-compose vs single-pass);
L3 only if banked structures compose *novel* procedures (Stage 3 bar, §J).

---

## C3 — Test-Time Program Synthesis with Compiled Execution (TTPS)

**Mechanism.** [HYPOTHESIS/HYPOTHESIS] Split cognition into *invention* and *execution*,
run by different substrates. The frozen model **synthesizes** a short program in a
constrained DSL whose primitives are frozen-model calls (attend-to(span), retrieve(),
compare(a,b), compose(f,g), branch-on(uncertainty)) — then a **deterministic
interpreter** (plain code, ~0F) *executes* the program, invoking the model only at
primitive call sites. The executed computation is therefore not the transformer's
forward pass but the synthesized program's control flow — this is "cognitive
compilation" literalized. A verifier role (same weights, separate instance) test-runs
candidate programs on support items and keeps the one with the best
rescue-rate-per-primitive-call. **Delta beyond the seed:** the directive's "internal
program synthesis" stops at generation; TTPS adds the compiled-execution substrate
(the program, not the model, is the computer) and the program-complexity audit that
distinguishes genuine compilation from paraphrased single-pass inference.

**Δθ=0 argument.** [FACT/PROPOSITION] The interpreter is external code; primitive calls
are ordinary forward passes; synthesis is generation. No parameter mutation anywhere.

**Closest prior art / novelty note.** [INFERENCE/INTERPRETATION] Program-synthesis
priors: DreamCoder (Ellis et al.), LEAP-style procedure learning — all *train*.
Test-time priors: PAL/PoT (program-aided LM — but the program is single-shot Python
for arithmetic, no search, no DSL over latent primitives). TTPS's bid: searched,
verified programs over *latent* primitives with compiled execution. Honest tier: N1
(combination) until the complexity audit shows non-trivial control flow doing causal
work.

**Systems cost sheet (track 6).**
- Per item: K candidate programs × ~1F (short generations) + interpreter ≈ 0F +
  V verification model-calls × 1F → **≈ (K+V)·F ≈ 8F** for K=V=4 [ILLUSTRATIVE].
- Latency: sequential generate→verify rounds (the binding cost; verification
  parallelizable across candidates).
- Memory: program store negligible; peak = one forward's A.
- **What the extra forwards buy:** the only candidate where extra FLOPs purchase a
  *different computer* — if the winning program branches/recurses, the executed
  computation is qualitatively new (L3 by construction, pending the audit).
  Falsifier of the cost claim (pre-registered): program-complexity audit — if ≥70%
  of winning programs are straight-line (no branch/recursion/loop) → compiled
  execution buys nothing over direct generation; halt.

> **Box 1 — unique support:** The complexity audit shows winning programs use
> branching/recursion/looping on ≥30% of items (pre-registered bar) AND replacing
> compiled execution with "model emits the program's final answer directly"
> (same synthesis FLOPs) removes the gain. No competitor executes a non-transformer
> computation — C1/C2/C4/C5/C6 all ultimately run transformer forward passes.
>
> **Box 2 — kill:** Winning programs are straight-line paraphrases of single-pass
> inference (audit fails the bar) OR direct-emission matches compiled execution
> (the program adds nothing). Then TTPS is "expensive chain-of-thought" —
> withdrawn. [HYPOTHESIS/HYPOTHESIS]

**Evidentiary target:** L3 directly (new computation: the interpreter's control flow)
— but L3 is licensed ONLY if the complexity audit passes; otherwise any gain is L1.

---

## C4 — Adaptive Strategy Routing (ASR)

**Mechanism.** [HYPOTHESIS/HYPOTHESIS] A meta-level router (frozen model in a probe
role: one cheap draft forward, read answer-token entropy + self-margin as difficulty
signals) assigns each instance a *computational strategy* from a pre-registered menu
{direct answer (1F), latent search (C6-style, ~2F), memory-augmented (C2-style, ~4F),
verify-heavy (generate + critic, ~6F)} — and, crucially, the routing *policy* itself
is discovered at test time by a bandit over strategies with an internal reward
(agreement + margin, target-free), not fixed by entropy thresholds. This is
"cognitive resource allocation" with a learned-at-test-time allocator. **Delta beyond
the seed:** the directive lists "adaptive routing between representations"; EAGER
(sprint N5) gates *tokens* by entropy with a fixed rule. ASR routes *strategies* and
learns the routing policy online — the claim is allocative efficiency (capability per
FLOP), the most directly systems-testable claim in this set.

**Δθ=0 argument.** [FACT/PROPOSITION] The bandit's policy is a temporary count table
(part of z_t, discarded); strategies are forward-pass patterns. No parameter update.

**Closest prior art / novelty note.** [INFERENCE/INTERPRETATION] EAGER (entropy-gated
compute), mixture-of-depths, early-exit — all gate *depth/tokens* with fixed rules.
ASR's bid: gating *strategies* with a test-time-discovered policy. Honest tier: N1;
the experiment that matters is the allocative-efficiency comparison (§F1), which no
prior runs against strategy-level routing.

**Systems cost sheet (track 6).**
- Per item: router probe 1F + routed strategy {1F, 2F, 4F, 6F}; expected ≈
  1 + (1−p_h)·1 + p_h·4 ≈ **3.2F** at p_h=0.3 heavy-rate [ILLUSTRATIVE].
- Latency: probe then routed pass (sequential); heavy tail for hard items.
- Memory: A (single forward at a time).
- **What the extra forwards buy:** *capability per FLOP* — the claim is that the same
  total FLOPs, reallocated by difficulty, beat uniform allocation. Directly falsifiable:
  pre-register total-FLOP parity vs uniform-heavy; if uniform wins, routing buys nothing.

> **Box 1 — unique support:** The strategy-shift assay (§F1): disabling the router
> (uniform strategy) at *matched total FLOPs* removes the gain (Δ>0, p<0.05) AND the
> router's difficulty predictions correlate with actual instance difficulty
> (AUC > 0.6 pre-registered) AND the test-time bandit policy beats the fixed
> entropy-threshold policy. No competitor learns an allocation policy — C1 controls,
> C2 remembers, C3 compiles, C5 cooperates, C6 branches, all at fixed budgets.
>
> **Box 2 — kill:** Router AUC ≤ 0.5 (chance) → pure overhead, withdrawn; OR
> uniform-heavy at matched total FLOPs ≥ routed (p≥0.05 for the difference) →
> allocation buys nothing, withdrawn. [HYPOTHESIS/HYPOTHESIS]

**Evidentiary target:** L2 (new strategy: difficulty-contingent computation); the
capability-per-FLOP framing keeps it honest at L2 even with large L1 gains.

---

## C5 — Latent-Channel Multi-Instance Cooperation (LCMIC)

**Mechanism.** [HYPOTHESIS/HYPOTHESIS] R frozen instances in fixed roles —
**proposer** (generates), **critic** (reads the proposer's *mid-layer residuals*, not
its output tokens, and emits a latent critique = a direction + a scalar doubt),
**arbiter** (reads both residual streams, issues the decision) — communicating through
a *latent channel* (activation tensors exchanged between roles' forward passes) over
D rounds, not through text. The mechanism claim: mid-layer residual exchange carries
more task-relevant bandwidth per FLOP than text debate (the critic sees *why* the
proposer is leaning, not just *what* it said). **Delta beyond the seed:** the
directive's "model-to-model cooperation" and the forced baseline debate/MoA are
*text*-channel. LCMIC's entire bid is the channel: same roles, same rounds, latent
vs text — the §F3 ablation isolates it.

**Δθ=0 argument.** [FACT/PROPOSITION] Roles are prompt-role framings of the frozen
model; exchanged tensors are activations. No parameter mutation.

**Closest prior art / novelty note.** [INFERENCE/INTERPRETATION] Debate (Irving et al.),
MoA (Wang et al.) — text-channel, the forced baselines. LCMIC's bid is strictly the
latent channel. Honest tier: N1 (multi-agent inference is well-trod); the F3 ablation
is the experiment that could promote the *channel* claim.

**Systems cost sheet (track 6).**
- Per item: R roles × D rounds × 1F = **R·D·F = 6F** for R=3, D=2 [ILLUSTRATIVE];
  latent exchange = memory copies O(D·R·d), negligible FLOPs.
- Latency: D sequential rounds (roles within a round parallelizable with extra
  hardware; on a single T4, sequential → 6 passes).
- Memory: R activation buffers if parallel else 1 (sequential) — pre-register the
  sequential schedule on free-tier VRAM.
- **What the extra forwards buy:** role-separated cognition at latent bandwidth.
  Falsifier of the cost claim (pre-registered §F3): text-only ablation at matched
  token budget matches latent-channel performance → the channel buys nothing; it's
  just debate.

> **Box 1 — unique support:** The §F3 ablation: latent-channel beats text-only
> (same roles/rounds/token budget, p<0.05) AND text-only ≈ debate/MoA forced
> baseline (the roles add nothing by themselves) AND the critic's latent doubt
> scalar predicts proposer errors above chance (the channel carries signal, not
> noise). No competitor uses inter-instance latent exchange — C1–C4, C6 are
> single-instance.
>
> **Box 2 — kill:** Latent ≈ text at matched budget (p≥0.05) → the channel is
> decorative; LCMIC reduces to debate and is culled (N1, no further spend).
> [HYPOTHESIS/HYPOTHESIS]

**Evidentiary target:** L2 (new strategy: latent-bandwidth cooperation); L1 gains
without the ablation win are just "more agents."

---

## C6 — Counterfactual Latent Branching (CLB)

**Mechanism.** [HYPOTHESIS/HYPOTHESIS] Inside *one* forward pass, fork the residual
stream at layer l\* into K counterfactual branches — each branch continues through
the remaining layers conditioned on a different hypothesized world-state
(e.g. different relational hypotheses, injected as branch-specific bias vectors that
are themselves generated, not hand-set) — then a **merge operator** (not selection:
e.g. consistency-weighted averaging of branch logits, or minimum-description-length
arbitration over branch trajectories) produces the decision. Unlike Best-of-N (K full
passes + pick-a-winner) the prefix is shared and the branches are *latent* (never
decoded); unlike Tree-of-Thoughts there is no text. The merge operator — arbitration
over counterfactual trajectories rather than selection among finished outputs — is
the claimed new computation. **Delta beyond the seed:** the directive's
"counterfactual latent computation" is a phrase; CLB specifies the fork point, the
branch-diversity guard (branches must stay diverse or parallelism is vacuous), and
the merge-vs-select ablation that decides whether the operator is new.

**Δθ=0 argument.** [FACT/PROPOSITION] Forking duplicates activations, not parameters;
branch biases are activation edits (hook pattern, cf. loop spec §2.2). θ untouched.

**Closest prior art / novelty note.** [INFERENCE/INTERPRETATION] Best-of-N /
self-consistency (output-level), ToT (text-level branching). CLB's bid: *intra-pass*
latent branching with an arbitration *operator*. Honest tier: N1 until the
merge-vs-select ablation shows the operator does work selection cannot.

**Systems cost sheet (track 6).**
- Per item: shared prefix (1−φ)·F + K branches × φ·F + merge O(K·d) ≈
  **(1+(K−1)φ)·F ≈ 2F** for K=4, φ=1/3 (last third of layers) [ILLUSTRATIVE].
  Branches batch in the batch dim → ≈1× wall-clock given VRAM.
- Latency: ≈ 1 forward (batched) — the cheapest parallel candidate per unit latency.
- Memory: **K branch activation buffers = K·A — the binding constraint.**
  Pre-register the VRAM gate: reduce K if OOM rather than silently shrinking φ.
- **What the extra forwards buy:** counterfactual evaluation at ~2F instead of K
  full passes, with an arbitration operator rather than a selection step.
  Falsifier of the cost claim (pre-registered): branches collapse (mean pairwise
  cos → 1) → parallelism buys nothing; or merge ≡ argmax (ablation) → the operator
  is Best-of-N in disguise.

> **Box 1 — unique support:** Branch diversity holds (mean pairwise cos < 0.9
> pre-registered) AND the merge-vs-select ablation favors the merge operator
> (p<0.05) AND CLB at ~2F beats Best-of-4 at 4F (more capability per FLOP —
> the systems claim). No competitor branches inside the pass — C4 routes between
> whole strategies, C5 parallelizes instances, C2/C3 serialize passes.
>
> **Box 2 — kill:** Branches collapse (diversity guard fails) OR merge ≡ argmax
> (operator reduces to selection) → CLB is "batched Best-of-N"; withdrawn.
> [HYPOTHESIS/HYPOTHESIS]

**Evidentiary target:** L2 (new strategy: intra-pass counterfactual arbitration);
L3 only if the merge operator is shown to compute something no selection can
(Stage 3 bar, §J).

---

## E summary for the synthesizer

| Candidate | H-map | Distinctive computation | Systems signature | Box status |
|---|---|---|---|---|
| C1 CLLC | H1 | feedback control w/ error bound | 2–6F, 2A, bound not luck | both answered |
| C2 ELM | H2 | cross-pass addressable latent RAM | T·F, +0.5MB, accumulates | both answered |
| C3 TTPS | H3 | synthesized program, compiled exec | ~8F, new computer | both answered |
| C4 ASR | H4 | test-time-learned strategy routing | ~3.2F, capability/FLOP | both answered |
| C5 LCMIC | H5 | latent-channel multi-role cooperation | 6F, channel ablation | both answered |
| C6 CLB | H5 | intra-pass counterfactual branching | ~2F, K·A memory-bound | both answered |

[INFERENCE/INTERPRETATION] The six are pairwise mechanism-distinct: no candidate's
Box-1 observation is predicted by any other candidate's mechanism — that is the
discrimination structure §F exploits. All Box-2 kills are pre-registrable as
written. None is [Supported] — all await falsification attempts (verdict standard).

---

# F. The 3 most scientifically important discriminating experiments

**Why these three** [INFERENCE/INTERPRETATION]: each separates *families* of
candidates (not candidates from baselines — that is §G's job), each targets L2+
evidence (strategy/capability, never raw L1 gains), and each has a pre-registered
cell in which a whole family is culled. Ordered by information value.

## F1 — The strategy-shift assay (adaptive-ablation at matched FLOPs)

**Discriminates:** adaptive families (C1, C4) vs structural families (C2, C3, C6)
vs "extra compute in disguise" (all). **Question:** does the gain come from a *new
strategy* (L2) or from *more FLOPs* (L1)?
**Design** [HYPOTHESIS/HYPOTHESIS]: for each candidate, run (a) full mechanism and
(b) the *adaptive-ablation*: identical total FLOPs, adaptive component disabled —
C1: inject the final control open-loop (no feedback); C4: uniform-heavy strategy at
matched expected FLOPs; C2: T passes with the buffer zeroed (recompute, no carryover);
C3: same synthesis FLOPs, direct emission instead of compiled execution; C5: same
roles/rounds, text channel; C6: K independent full passes + argmax (the Best-of-K it
claims to beat). Pre-registered statistic: adaptive-ablation Δ =
ΔM_full − ΔM_ablated, McNemar on the paired difference, p<0.05.
**Why it discriminates** [INFERENCE/INTERPRETATION]: a positive Δ with the FLOPs
identical *proves the strategy did the work* — the only L2-licensed inference in
this program's history. A zero Δ demotes the candidate to L1 ("more compute helped")
regardless of headline gains — no silent level-crossing. **Culls:** any candidate
with Δ ≤ 0 is demoted out of the "new mechanism" race (it may survive as L1
engineering only).

## F2 — Cross-family transfer under frozen protocol (generality assay)

**Discriminates:** general architectures (C2, C3, C6 claim generality) from
task-bound steering (C1 as specified, C5 as instantiated). **Question:** is the
candidate a *cognitive architecture* or a *task-specific trick*?
**Design** [HYPOTHESIS/HYPOTHESIS]: the identical mechanism protocol — hyperparameters
frozen, only task-specific support data permitted per the information boundary
(loop spec §1 pattern) — applied to three pre-registered task families: (i) relational
2-hop (home), (ii) algorithmic (multi-step symbolic with hidden intermediates),
(iii) planning (hidden-structure navigation). Each family carries its own forced
baselines and negative controls (§G conditions, per family). Transfer criterion
(pre-registered): beats compute-matched best fixed baseline on ≥2/3 families AND
passes its Box-1 fingerprint on each winning family.
**Why it discriminates** [INFERENCE/INTERPRETATION]: the A–J synthesis asks for a
*successor architecture*, not a benchmark-specific hack. A mechanism that wins only
at home is task-specific steering — valuable (L1) but not the program's objective.
**Culls:** candidates winning <2 families are re-scoped to "task-specific method,"
removed from the Stage-3 track (§J).

## F3 — The latent-channel ablation (C5 vs debate/MoA)

**Discriminates:** C5's channel claim from the forced text-debate baseline; secondarily
tests whether *any* multi-instance candidate needs its exotic channel.
**Design** [HYPOTHESIS/HYPOTHESIS]: three arms at matched token budget and matched
FLOPs on the NTDP task (§G): (a) C5 full (latent exchange), (b) C5-text (identical
roles/rounds, text-only communication), (c) debate/MoA forced baseline. Pre-registered
predictions: (a)>(b) p<0.05 AND (b)≈(c) p≥0.05 ⇒ the latent channel is the mechanism;
(a)≈(b) ⇒ C5 reduces to debate (culled, N1). Secondary: critic's latent doubt scalar
predicts proposer errors above chance (AUC>0.55) — the channel carries signal.
**Why it discriminates** [INFERENCE/INTERPRETATION]: this is the cheapest
family-killing experiment in the set (~6F/item × 3 arms, one task family) and it
adjudicates the entire "cooperation" direction: if latent bandwidth adds nothing over
text, the H5-cooperation branch of the design space is closed without further spend.

---

# G. The killer experiment — Novel-Task Discovery Protocol (NTDP), spec sketch

**Role in program:** pre-registered program-level gate on synthesis landing (spec §4).
Success = convincing evidence for a new inference-time cognitive mechanism (L2
required, L3 claimed only if the L3 sub-bars pass).

## G1. Hypothesis

**H_G** [HYPOTHESIS/HYPOTHESIS]: *A frozen model (θ_after=θ_before) equipped with
candidate mechanism C\* discovers and executes a task-appropriate computational
procedure on a novel task family — constructed after mechanism freeze, with hidden
structure no fixed-inference method exploits — achieving decision accuracy no
FLOPs-matched fixed method achieves, with pre-registered evidence that the
*strategy* (not just the score) is new.*

## G2. Benchmark conditions (track 9 duty)

- **Task construction:** the benchmark scientist (track 9) constructs the task family
  *after* all candidate mechanisms are frozen (constructor independence — the
  constructor does not see mechanism internals). Hidden compositional or
  hidden-structure design (the track-9 charter: ordinary prompting and fixed
  steering must be insufficient *by construction*). N ≥ 80 items; 40–70% headroom
  on the frozen backbone verified *before* mechanism evaluation (Law #9); items
  without verified headroom excluded and logged.
- **Negative controls (every run):** unintervened backbone; B_perp (random orthogonal
  direction, norm-matched); B_wrong (distractor direction); **shuffled-label control**
  (swap target/foil roles — a true mechanism must not "rescue" toward the wrong
  label; catches readout-bias artifacts, cf. the §CHALLENGE); random-selection
  ablation of the candidate's search component.
- **Forced baselines — all at matched total inference FLOPs per item** (track 6/11
  parity rule; FLOPs counted, not passes assumed): best fixed prompt + CoT; static
  CAA; Best-of-N / self-consistency over outputs; text debate / MoA; STARS; DEER;
  ∇-Reasoner; **next-scale frozen model** (e.g. pythia-410m vs 160m) with the
  compute-parity accounting of §SC stated in the pre-registration.
- **Evidentiary level:** L2 required for "survive"; L3 additionally requires the L3
  sub-bars (G4). A bare L1 win is Branch F2, not success — no silent crossing.

## G3. Conditions (per candidate C\*)

M1 unintervened · M2 best fixed prompt+CoT · M3 static CAA · M4 Best-of-N (FLOP-matched)
· M5 debate/MoA (FLOP-matched) · M6 STARS · M7 DEER · M8 ∇-Reasoner · M9 next-scale
frozen model · M10 C\* full · M11 C\* adaptive-ablation (§F1) · M12–M14 negative
controls (B_perp, B_wrong, shuffled-label). Primary endpoint: ΔM decision changes,
exact McNemar; secondary: per-sample harm ledger (anti-steerable fraction), Box-1
fingerprint check (per-candidate, §E), per-instance FLOP accounting.

## G4. Decision tree (exhaustive per M5.2; verdicts per the binding standard)

- **Branch S — KILLER SUCCESS.** ALL hold: (i) ΔM_C\* > 0 vs M1, p<0.05; (ii) C\*
  beats EACH of M2–M8 at matched FLOPs, p<0.05; (iii) M9 does not beat C\* beyond
  the pre-registered parameter-equivalence margin (§SC — else Branch F3); (iv) all
  negative controls null (M12–M14 ΔM=0, p≥0.05; shuffled-label shows no rescue);
  (v) Box-1 fingerprint check passes; (vi) per-sample harm ledger: anti-steerable
  fraction < 15% (else Branch F4). → **Verdict: Supported** (L2 licensed). **L3
  additionally** iff: (a) M2–M9 all ≈ chance-class performance on a pre-registered
  *hard subset* (items the constructor certifies require structure discovery) while
  C\* succeeds reliably there; (b) the track-12 discovery criterion is met (procedure
  novel to the model per held-out-family operationalization, verified by independent
  oracle). → L3 licensed; program advances to §J Stage 2.
- **Branch F1 — beaten by fixed methods.** C\* ≤ best of M2–M8 at matched FLOPs
  (all p≥0.05 for the difference). → **Not supported.** Feeds experiment H.
- **Branch F2 — gain without strategy.** (i)–(iv) hold but (v) fingerprint fails.
  → **Underdetermined** (L1 gain, mechanism unproven). C\* continues ONLY as L1
  engineering; the "new mechanism" claim is not licensed.
- **Branch F3 — parameter substitution.** M9 (bigger frozen model) beats C\* at
  matched inference FLOPs beyond the margin. → **Inconclusive** for novelty: the
  capability exists in weights; C\* is not licensed as *new computation*.
  (Track-11 rule: this is not a failure of C\* as engineering — it is a failure of
  the novelty claim.)
- **Branch F4 — harm.** Anti-steerable fraction ≥ 15% with net ΔM>0.
  → **Not supported** as a cognitive mechanism ("wins on average while harming a
  sixth of instances" is inadmissible per sprint N3); returns for re-design, not
  advancement.
- **Degenerate cells.** Zero variance (all items correct/wrong under some arm) →
  statistic undefined → **Inconclusive**, task re-constructed (M5.3: the cell is
  named, not left to executor discretion). b=c=0 throughout ⇒ p=1.0 exactly
  (vacuous "p<0.05" cell impossible — one-line proof per M5.4).

## G5. Forward-pass budget (pre-registered cap)

Per item per candidate: F_C\* ≤ 8F (hard cap; candidates declaring above 8F/item are
inadmissible for G — the "100× compute" trap, track-6 duty). Total per candidate:
N=80 × 14 conditions × ≤8F ≈ ≤ 9,000F [ILLUSTRATIVE]; at ~0.5 s/pass (410m-class,
T4) ≈ **3–5 h T4 per candidate** — schedulable across Kaggle sessions on the free
tier. Pilot: 20 items × full conditions first; full run only if no degenerate cell
fires on the pilot.

---

# H. The failure experiment — explicit license to kill the mechanism family

**Role in program:** pre-registered program-level gate with **explicit license to
kill** (spec §4; mentor P3). H runs the *family-level* adjudication: G tests
candidates; H tests whether the family deserves to exist.

## H1. Hypothesis (the family claim)

**H_H** [HYPOTHESIS/HYPOTHESIS]: *There exists a temporary-computation mechanism
(any of C1–C6, or a pre-registered composition of at most two) that changes the
computational strategy (L2) or creates qualitatively new capability (L3) on the
NTDP at matched inference FLOPs vs the forced-baseline set.*

## H2. Design

All surviving candidates (post-§F culling) run the full NTDP (§G2–G3) with one
addition: the **family-ablation arm** — the *best* fixed method per item
(post-hoc oracle over M2–M8, labels used only for this ceiling measurement, cf.
EXP070 precedent): if no candidate beats even the oracle-fixed ceiling, the family
cannot claim headroom exists. Benchmark conditions, negative controls, forced
baselines, harm ledgers: identical to §G2 (track-9 duty discharged once, reused).

## H3. The kill trigger — READ THIS AS LICENSE

**KILL TRIGGER (pre-registered):** the mechanism family is **Refuted** as a
cognitive-augmentation program iff ALL of the following hold on the NTDP:
1. Every candidate lands in G-Branch F1, F2, or F4 (no Branch S anywhere);
2. No candidate passes its Box-1 fingerprint check (no L2 evidence for any
   mechanism — gains, if any, are L1-only);
3. The per-sample harm ledgers show no rescuable subset (gains unconcentrated —
   there is no "it works on a discoverable subset" retreat);
4. The oracle-fixed ceiling (H2 addition) is not beaten by any candidate
   (no headroom for *any* fixed-computation selector to exploit).

**THE LICENSE:** if the kill trigger fires, the program **closes the
temporary-computation mechanism arm**: no new C-variants, no rescues by re-tuning,
no "one more architecture." The A–J "new mechanism" claim is withdrawn in writing.
Remaining resources redirect to (a) publishing the negative result — *why*
temporary computation cannot add capability under Δθ=0 is itself a boundary claim
of publishable value (the program's most defensable asset class, per the kill
culture of G1/EXP079); (b) mechanistic understanding of the boundary. **This
license is pre-registered and executes without re-approval.** Theory preservation
— keeping the family alive for elegance, ambition, or sunk cost — is the named
forbidden move, and this paragraph is its antidote.

## H4. Survive / demote branches (exhaustive)

- **Survive:** any candidate hits G-Branch S (L2+) → family survives **narrowed**
  to the surviving mechanism(s); all others culled Friday-style (sprint N5
  precedent). Program advances to §J Stage 2 with the survivor(s) only.
- **Demote (partial):** ≥1 candidate shows replicable L1 gains (Branch F2,
  fingerprint failed) with harm < 15% → the family survives ONLY as an
  **inference-improvement engineering program**, explicitly demoted from the
  superintelligence objective. The L2/L3 claim is withdrawn; no Stage 2/3.
  [INFERENCE/INTERPRETATION: this is the "useful but not new" cell — honest,
  publishable, and not the program's organizing question.]
- **Degenerate:** NTDP itself fails construction (no headroom achievable, or all
  forced baselines ≈ chance AND all candidates ≈ chance — the task is broken, not
  the family) → **Inconclusive**; track 9 reconstructs; H re-runs once. A second
  consecutive degenerate construction retires H in favor of a redesigned gate
  (logged, Law #8 — never silently dropped).

## H5. Forward-pass budget

Shared NTDP infrastructure with G (constructor, headroom verification, baselines
M1–M9 run once, reused across candidates): marginal cost per additional candidate
≈ N=80 × 4 arms (M10/M11/M12–14 subset) × ≤8F ≈ 2,500F ≈ **~1 h T4**
[ILLUSTRATIVE]. The family-level verdict is therefore *cheaper* than any single
candidate's G run — killing is economical, which is the point.

---

# J. Three-stage roadmap (provisional — pending Cluster A I-input, §0.3)

**Provisional operational definition** [SPECULATION/CONJECTURE] (to be reconciled
with track 5's deliverable I; the synthesizer must substitute Cluster A's text):
*"Superintelligent augmentation" = a Δθ=0 mechanism that (a) passes G-Branch S
(L2+ on the NTDP), (b) transfers across ≥3 task families (§F2), and (c) demonstrates
at least one L3 sub-bar (G4a/b): a task class where frozen backbone + all fixed
methods ≈ chance but the mechanism succeeds reliably, or a model-novel procedure
verified by an independent oracle.* No hype terms ("superhuman," "revolutionary")
appear in any gate criterion.

## Stage 1 — Prove/disprove the mechanism

**Work:** §F culling (F1–F3) → G (NTDP per surviving candidate) → H (family
adjudication). **Pre-registered gate:** advance iff ≥1 G-Branch S (L2+); close
iff H kill trigger (§H3); demote iff H-Demote branch. **No candidate reaches
Stage 2 on L1 gains.** [INFERENCE/INTERPRETATION: Stage 1 is the falsification
funnel; its output is at most two surviving mechanisms, more likely zero.]

## Stage 2 — Cross-task / general transfer

**Work:** survivor(s) run §F2 across ≥3 pre-registered task families (relational,
algorithmic, planning + one track-9-constructed hidden-structure family), each with
own forced baselines, negative controls, harm ledgers. **Pre-registered gate:**
transfer bar = beats compute-matched best fixed baseline on ≥2/3 families AND Box-1
fingerprint passes on each winning family AND harm < 15% throughout. **Fail:**
mechanism re-scoped to "task-specific method" (honest, publishable); program does
not advance it. **Pass:** Stage 3.

## Stage 3 — Qualitatively new capabilities

**Work:** the L3 sub-bars, pre-registered per mechanism: (a) **chance-bar:** task
class where backbone + all fixed methods ≈ chance (pre-registered chance baseline +
margin) but the mechanism succeeds reliably (replicated, N≥80); (b) **discovery
bar** (track 12): the mechanism outputs a procedure/algorithm/hypothesis operationally
novel to the model (held-out-family test) and verified by an independent oracle;
(c) **scaling bar** (track 11): capability-vs-inference-FLOPs curve for the mechanism
vs parameter scaling, with the §SC compute-parity falsifier named — the mechanism
must show a regime where inference-FLOPs beat parameter-FLOPs or the "new
capability" claim is not licensed as *efficient*. **Gate:** all three sub-bars, or
the L3 claim is not made. [SPECULATION/CONJECTURE: Stage 3 may require
[NEEDS-COMPUTE] beyond free-tier VRAM for larger backbones — flagged honestly;
Stage 1–2 fit the free tier per §G5/H5.]

---

# Track-duty appendices

## Systems (track 6) — cross-candidate cost comparison

| Candidate | F/item [ILLUS.] | Latency | Memory | Energy | Buys (one line) |
|---|---|---|---|---|---|
| C1 CLLC | 2–6F | ~2 sequential fwd | 2A | 2–6× | error *bounds*, not luck |
| C2 ELM | T·F (3–8) | T sequential | A + 0.5MB | T× | accumulating state |
| C3 TTPS | ~8F | gen→verify rounds | A | 8× | a *different computer* |
| C4 ASR | ~3.2F expected | probe + routed | A | ~3× | capability per FLOP |
| C5 LCMIC | 6F | D rounds | 1–R·A | 6× | latent-bandwidth roles |
| C6 CLB | ~2F | ~1 fwd (batched) | K·A (binding!) | ~2× | counterfactuals at 2F |

[INFERENCE/INTERPRETATION] C6 dominates on latency-per-FLOP; C4 dominates on
expected-FLOP efficiency; C1 is the only one selling guarantees; C2's memory cost
is negligible but its latency is the worst. Any proposal needing >8F/item is
inadmissible for G — stated as law, not guidance.

## Benchmark (track 9) — conditions register for G/H

Constructor independence (task built after mechanism freeze); N≥80; 40–70% headroom
verified pre-evaluation; negative controls {unintervened, B_perp, B_wrong,
shuffled-label, random-selection ablation}; forced baselines {best prompt+CoT,
static CAA, Best-of-N, debate/MoA, STARS, DEER, ∇-Reasoner, next-scale frozen model}
— **all FLOP-matched**; endpoints ΔM (exact McNemar) + harm ledger + fingerprint;
evidentiary level L2-required/L3-gated. A benchmark lacking any element is a
measurement, not a gate (track-9 charter).

## Scaling (track 11) — compute-parity law

**Standing rule** [INFERENCE/PROPOSITION]: no "small+X ≈ bigger" claim is admitted
without (i) total inference FLOPs/item for small+X **and** for the bigger model
stated in the pre-registration; (ii) the accounting frame named (training FLOPs
amortized vs excluded — both reported, neither hidden); (iii) the task distribution
named; (iv) the **falsifier**: *if bigger+X beats small+X by ≥ X's gain on small,
the trade is not systematic — X is not a parameter substitute, it is a
compute-multiplier.* One benchmark result is never parameter equivalence.
G-Branch F3 is this rule's enforcement cell.

---

# CHALLENGE (knowledge-protocol §2 obligation)

**Target:** the live [INTERPRETATION] that the output-bridge rescues (+10/+16.67/
+23.33pp, EXP077/070/066) mark an "output-side room" where the transferable
mechanism lives (synthesis spec §2; sprint Idea 7's premise).

**The challenge** [INFERENCE/INTERPRETATION]: an unembedding-space direction injected
at the answer position is mathematically adjacent to adding a constant logit bias
toward the target token. If the "bridge" is ≈ the target token's unembedding vector
(or a class-level readout tilt), its rescues are **readout bias, not a mechanism** —
it changes decisions without changing computation, and the O5 margin-invalidation
logic applies one level up: *decision changes from output-space injection are not
evidence of improved reasoning either.* The "output-side room" would then be a
measurement artifact of the binary answer structure, and EXP078's "causal power lies
outside the concept subspace" would re-read as "the causal power lies in the readout,
which was never the question."

**Cheapest falsifying experiment ($0 GPU)** [HYPOTHESIS/HYPOTHESIS]: on archived
EXP065/066/077 artifacts — (a) cos(bridge direction, unembedding row of t_target):
if ≈1, it is a target boost, full stop; (b) correct-item flip analysis: does the
bridge flip correct→wrong at symmetric rates (bias) or only wrong→correct (rescue)?
(c) **label-shuffle test**: re-score archived items with target/foil roles swapped —
a true relational mechanism must now *hurt*; a readout tilt still "helps" toward the
original target token. **Kill criterion for the interpretation:** (a) cos ≈ 1 OR
(c) shows rescue-persistence under label shuffling → the bridge is readout bias;
the "output-side mechanism" reading is withdrawn, the positive-control status of the
bridge is revoked (it remains a *rescue* positive control, not a *mechanism*
positive control), and Idea 7's premise is re-examined. If (a)–(c) all favor a true
mechanism, the output-side lead stands strengthened — the challenge is designed to
lose cleanly if wrong.

# IDEA (knowledge-protocol §2 obligation)

**Test-time erasure (subtraction-as-augmentation)** [SPECULATION/CONJECTURE]: the
corpus suggests but never tests *removing* representational components as the
intervention. G1 [FACT/OBSERVATION]: B_wrong is QK-prominent (0.4885) yet causally
inert (ΔM=0) — a distractor component the model carries but does not use. Jorgensen
centring (sprint Idea 5) removes shared components as hygiene. Untested step: **erase
as the mechanism** — project the residual stream *away from* the top distractor
direction (or top residual PC) at test time: h ← h − P_distractor(h). If errors are
caused by interference rather than missing signal, erasure rescues where addition
failed — a different mechanism class (ablation-as-augmentation) from everything in
§E. **Why it matters:** it is the only proposal that treats the model's *own*
prominent-but-inert components as the obstacle, and it is falsifiable for $0.
**Cheapest falsifying experiment ($0 GPU):** necessary-condition gate on archived
EXP065/066 activations — do *wrong* items carry higher projection energy onto the
distractor direction than *correct* items? If no energy difference: erasure cannot
selectively help → **idea dead before any GPU** (kill criterion part 1). If yes:
pilot GPU run (~30 min T4): erase-distractor vs erase-B_perp (control) vs bridge;
**kill criterion part 2:** ΔM_erase ≤ 0 (McNemar p≥0.05) while bridge rescues →
dead. **Novelty:** corpus grep shows no erasure-as-intervention experiment
(EXP-ledger: all interventions are additive); N1-adjacent at best, but the $0
necessary-condition gate makes it the cheapest idea in this report.

---

# Synthesizer handoff note

1. **Both boxes answered for all six candidates** (§E table) — commissioning standard met.
2. **H1–H5 mapping:** C1→H1, C2→H2, C3→H3, C4→H4, C5+C6→H5 (two distinct H5
   inventions; keep one or both — C6 is cheaper, C5 is more radical).
3. **I-input missing:** §J uses a provisional operational definition
   [SPECULATION/CONJECTURE]; substitute Cluster A's track-5 deliverable I before
   program adoption (Law #14 review of the assembled synthesis).
4. **G/H pre-registration triggers on this synthesis landing** (spec §4) — the
   pre-registration agent should lift §G2–G5/§H2–H5 verbatim; decision trees are
   exhaustive per M5.2 (degenerate cells named).
5. **No GPU used; no signed protocols touched; no numbers invented.** All
   cost figures are [ILLUSTRATIVE] with derivations shown (M3). All candidate
   claims are [HYPOTHESIS]/[SPECULATION] — verdicts pending falsification.
6. **Recommended cull order if compute binds:** F3 first (cheapest family-kill),
   then F1 (demotes L1-disguised candidates), then G pilots.
7. **Open question for the mentor:** whether the §CHALLENGE's label-shuffle test,
   if it confirms readout bias, demotes the bridge from "positive control" across
   all signed protocols that used it (EXP065/066/070/077) — flagged, not decided
   here.

*End of Cluster C report. LOG-140 dispatch discharged.*
