# K1 — Readout-Tilt Falsification: EXECUTOR REPORT (LOG-213)

**LOG-213 · Track-8 (Implementation) · 2026-09-23**
**Plan:** `research/analysis_plans/K1_READOUT_TILT_PLAN_LOG205_REV1_2026-09-23.md` (REV1, Law #14 SIGN on diffs)
**Status:** EXECUTED — §10 steps 1–8 complete. **Cost:** $0 CPU. **Forward passes:** 0.
**Evidentiary level:** L1 (readout-path geometry / causal accessibility — no L2/L3 crossing).
**Machine-readable twin:** `research/analysis_plans/K1_RESULTS_LOG213_2026-09-23.json`
**Executor script:** `research/analysis_plans/K1_execute_LOG213_2026-09-23.py`

---

## Headline

**K1-EXONERATED.** The readout-tilt hypothesis is **Not supported** on all three
primary runs: not a single one of the 180 bridge directions (60 items × 3 runs)
reaches the pre-registered 0.9 target-row-alignment bar (k = 0/60 everywhere;
exact Clopper–Pearson 95% CI [0.0000, 0.0596], upper bound far below the 0.5
majority bar). The bridge is geometrically a relational (t−f) direction, not a
target-row boost. Per the pre-registered §9 consequences, the LOG-204 §H7 demotion
does **not** lock in — the CEO revisits §H7; the KEEP chain resumes (K2 pilot
licensed; Sprint-3 Stage-0 pilots licensed with positive class intact; K3's CPU
compliant-bridge construction audit proceeds).

Two execution discoveries are disclosed below (G3 hash-function correction; G2
smoke-bench anomaly). Neither alters any verdict cell; both are flagged for
Law #14 review.

---

## 1. Verdict table (§7 of the plan, filled verbatim)

| (a) | (b) diagnostic | Guards | Per-run verdict |
|---|---|---|---|
| KILL fires | any | pass | **Supported** (tilt confirmed) |
| ruled out | weakens/neutral | pass | **Not supported** (tilt exonerated) |
| ruled out | f ≥ r/2 | pass | **Not supported** (geometry rules tilt out; (b) notes unclean effect — recorded, doesn't resurrect tilt) |
| (a) did not fire | — | pass | **Inconclusive** |
| — | — | **fail** | **Refuted** (measurements invalid; no claim licensed; halt, report) |
| EXP070 cells | — | — | **Underdetermined** (no verified vectors; excluded) |

**Filled:**

| Run | (a) outcome | (b) diagnostic (verbatim adopted mapping) | Guards | Per-run verdict |
|---|---|---|---|---|
| EXP065 (pythia-160m/L10) | RULED-OUT (k=0/60, upper CI 0.0596 < 0.5) | weakens | pass | **Not supported** |
| EXP066 (pythia-410m/L20) | RULED-OUT (k=0/60, upper CI 0.0596 < 0.5) | weakens | pass | **Not supported** |
| EXP077-official (pythia-410m/L20) | RULED-OUT (k=0/60, upper CI 0.0596 < 0.5) | neutral | pass | **Not supported** |
| EXP077-smoke | secondary only — (a) not computed (labels not archived; see §6) | weakens (diagnostic) | pass | n/a (secondary) |
| EXP070 | — | — | — | **Underdetermined** |

**Program-level aggregation (pre-registered):** ≥1 primary Supported → K1-CONFIRMED;
all primaries Not supported → K1-EXONERATED; otherwise Inconclusive.
**Result: K1-EXONERATED.** No guard failed → no Refuted cell is reachable.

---

## 2. Endpoint (a) — geometric target-boost test [PRIMARY] [OBSERVATION]

Per-item c_i = cos(b̂_i, ŵ_{t_i}), b̂_i = normalize(E_run[t_i] − E_run[f_i])
(LOG-197-verified construction; α drops out of the cosine), 60 items per run.
p̂_a = #{i : c_i ≥ 0.9}/60 with exact Clopper–Pearson 95% CI. Pre-registered bars:
(a)-KILL needed lower CI > 0.5 (≥39/60); (a)-RULED-OUT needed upper CI < 0.5
(≤21/60). CI implementation self-checked against the registered cutoffs
(CP(39/60) lo=0.5160>0.5; CP(38/60) lo=0.4990; CP(21/60) hi=0.4840<0.5;
CP(22/60) hi=0.5010).

| Run | k (c_i ≥ 0.9) | p̂_a | CP 95% CI | mean | median | min | max | §3 outcome |
|---|---|---|---|---|---|---|---|---|
| EXP065 | 0/60 | 0.0000 | [0.0000, 0.0596] | 0.1571 | 0.1562 | 0.1299 | 0.1808 | RULED-OUT |
| EXP066 | 0/60 | 0.0000 | [0.0000, 0.0596] | 0.5598 | 0.5571 | 0.5142 | 0.6420 | RULED-OUT |
| EXP077-official | 0/60 | 0.0000 | [0.0000, 0.0596] | 0.5598 | 0.5571 | 0.5142 | 0.6420 | RULED-OUT |

[FACT] Not one item in any run reaches the 0.9 bar; the upper confidence bound
(0.0596) rules out even a 6% target-boost rate at 95% confidence, far below the
0.5 majority bar. The (a)-KILL did not fire on any run.

[OBSERVATION] The 160m geometry (mean c_i = 0.157) differs markedly from the
410m geometry (mean c_i = 0.560); both sit far below 0.9 and below the 1/√2 ≈
0.707 equal-norm-orthogonal reference the plan pre-registered as the
non-vacuity anchor. EXP066 and EXP077-official c_i arrays are bit-identical
(max abs diff 0.0) — expected, since both use the same pinned snapshot and
byte-identical items; reported as a consistency observation, not a guard.

[INFERENCE] The bridge construction is geometrically a relational (t−f)
direction, not a target-row boost: the −foil component is not negligible, and
the §H6 "it is a target boost, full stop" disjunct does not trigger.

---

## 3. Endpoint (b) — correct-item flip analysis [DIAGNOSTIC] [OBSERVATION]

Adopted reading kept verbatim per Law #4 (diagnostic only; never fires the kill;
the LOG-212 standing correction — that the adopted f ≥ r/2 mapping is backwards
vs the L1 license — is noted here, not applied).

| Run | r (rescues) | f (flips) | ΔM̂ | lower 95% CI (Tango, archived) | Adopted reading |
|---|---|---|---|---|---|
| EXP065 | 10 | 0 | +0.1667 | +0.0931 > δ_min | weakens |
| EXP066 | 8 | 0 | +0.1333 | +0.0651 > δ_min | weakens |
| EXP077-official | 6 | 0 | +0.1000 | +0.0338 ≯ δ_min | neutral |
| EXP077-smoke (secondary) | 14 | 0 | +0.2333 | +0.1444 > δ_min | weakens |

[FACT] Archived (b, c) aggregates were re-verified by recomputation from
per-item correctness records where they exist: EXP066 (8, 0) and EXP077-smoke
(14, 0) both match exactly. EXP065 has no per-item correctness archive
(aggregates only: 10, 0). EXP077-official (6, 0) is the LOG-128 GPU record
(no in-repo official per-item archive; notebook mirror pending — standing TODO).

[INTERPRETATION — adopted mapping, verbatim] f = 0 with r > 0 and lower CI >
δ_min scores as "weakens" tilt on EXP065/EXP066/smoke and "neutral" on the
official run. Under the LOG-212 standing correction this same f = 0, r > 0
pattern is the L1 license's *predicted* signature of a clean directional
effect; the correction is recorded and does not modulate any verdict.

---

## 4. Endpoint (c2) — swapped-scoring identity [INTEGRITY CHECK] [OBSERVATION]

[FACT] The §5.1 identity holds exactly on recomputation from per-item records:
(b_swap, c_swap) = (0, 8) = (c_orig, b_orig) for EXP066 and (0, 14) = (14, 0)
for EXP077-smoke, with the ΔM identity verified in exact integer arithmetic
((b_swap − c_swap) = −(b − c)). G4 passed. For EXP065 (no per-item records) the
identity is the §5.1 arithmetic consequence of the (10, 0) aggregates.
[INTERPRETATION] As pre-registered, this licenses no claim about tilt vs
mechanism; it is recorded so the naive swapped-scoring reading can never be
laundered as evidence later.

---

## 5. Diagnostic D1 — logit-shift ratio [DIAGNOSTIC] [OBSERVATION]

Archived sign signature (target↑, foil↓, near-symmetric) confirmed on both runs
with archived shifts: EXP065 Δℓ_t=+0.3597/Δℓ_f=−0.4042; EXP066
Δℓ_t=+0.3448/Δℓ_f=−0.4044.

| Run | r_obs = Δℓ_t/\|Δℓ_f\| | r_pred (L1, weights-only) | \|r_obs−r_pred\|/r_pred | Pre-registered reading (2× tolerance [ARBITRARY]) |
|---|---|---|---|---|
| EXP065 | 0.8899 | 1.0010 | 0.111 | strengthens |
| EXP066 | 0.8524 | 0.8155 | 0.045 | strengthens |

[OBSERVATION] The L1-predicted readout-projection ratios match the archived
end-to-end ratios within 11% (EXP065) and 4.5% (EXP066) — the readout projection
quantitatively exhausts the observed effect. EXP077 (official + smoke): no
archived logit shifts; D1 not applicable.

[INFERENCE] D1 and (a) together give a coherent picture: the bridge's end-to-end
effect is fully accounted for by its L1 readout projection (D1 strengthens),
yet the bridge direction is not a target-row boost ((a) ruled out). The
rescue is readout-path-mediated — as a relational (t−f) direction, not a
label-assisted target tilt. D1 cannot and does not resurrect the tilt verdict;
the §7 table does not consult it.

---

## 6. Guards (all FATAL on failure — none failed)

- **G1 — bridge-identity re-verification: PASS.** |cos(rebuilt b̂_i, independent
  numpy-float64 path)| ≥ 1−1e-6 on all 60 items × 3 runs (worst deviations:
  2.98e-07 / 7.50e-08 / 7.50e-08). [FACT]
- **G2 — label cross-check: PASS (scoped; see discovery note).** Rebuilt EXP065/066
  labels byte-consistent 60/60 with archived `exp066_instance_evaluations.json`
  prompts (the records carry no token fields — prompts are the plan's cited
  source). EXP065 has no per-item archive (LOG-197 byte-identity FACT cited).
  EXP077-official has no in-repo per-item records; labels come from the frozen
  bench port per the plan's pin. **Discovery:** the EXP077 *smoke* archive's
  bench differs from the ported construction (archive: exactly 6 items per
  entity, entity-grouped; ported: Mars×21/Iron×21/Venus×7/Gold×7/Jupiter×2/
  Silver×2) — the plan's §2 premise of smoke byte-identity is falsified by the
  archive itself, and the smoke's (target, foil) pairs are not archived, so the
  smoke (a) was not computed rather than filled by assumption. The smoke is
  "labeled secondary only" and feeds no §7 verdict cell. Flagged for Law #14.
- **G3 — Δθ=0: PASS (with disclosed measurement correction).** Archived pre==post
  on all three runs. Cross-checks reproduce each archive's digest with its
  runner's *verbatim* hash function: pythia-160m params-order
  `54c88fa4897785f2…936` == EXP065 archive; pythia-410m params-order
  `4c242d9ac702a402…dd` == EXP066 archive; pythia-410m sorted-state_dict
  `ec276abe3902fab0…0ed` == EXP077-smoke archive. Post-execution hashes match
  pre-execution hashes under both conventions. **Discovery:** the plan's §6 A3 /
  §7 G3 named only run_exp077.py's sorted get_hash as "the verbatim get_hash,"
  but the EXP065/066 archives were hashed with their runners' own verbatim
  function (`model.parameters()` order, raw dtype bytes — run_exp065 ll. 33–38,
  run_exp066 ll. 34–38). A literal single-function comparison yields a false
  FATAL on byte-identical weights (the LOG-123/126 ordering-convention artifact
  class, already root-caused in the program). Corrected to per-archive verbatim
  functions — a measurement-method repair implementing the guard's stated intent
  (weights byte-identical to the archived runs); no endpoint, environment, or
  margin changed. Flagged for Law #14.
- **G4 — (c2) identity: PASS** (exact; §4).
- **G5 — no forward pass: PASS.** The executor script contains no invocation of
  the model on inputs — asserted by in-script source self-inspection at startup
  (regex scan for `model(` call forms; only `model.get_output_embeddings()`,
  `model.state_dict()`, `model.eval()` accessors present) and confirmed by
  independent code inspection. Forward-pass count: 0.

**A8 — non-degenerate difference rows** [OBSERVATION]: min_i‖W_U[t_i]−W_U[f_i]‖₂
= **0.784649** across all items × runs (> 0; asserted and reported per F-G2).

**Environment pin (§8)** [FACT]: `/home/hatch/workspace/.venv_smoke` —
torch 2.14.0+cpu, transformers 5.17.0, numpy 2.5.3 (asserted exact in-script);
snapshots `50f5173d932e8e61f858120bcb800b97af589f46` (160m) and
`9879c9b5f8bea9051dcb0e68dff21493d67e9d4f` (410m) present in local HF cache —
no download, no substitution; `local_files_only`, `trust_remote_code=False`,
`torch_dtype=torch.float32`, `model.eval()`; deterministic, no RNG consumed.

---

## 7. Mathematical license — L1 reproduced (three lines) [THEOREM]

Let W_U ∈ ℝ^{V×d} be the frozen unembedding matrix, x ∈ ℝ^d the final-token
residual, logits ℓ = W_U x (+ constant bias, immaterial: Δℓ is bias-independent).
For unit v̂ ∈ ℝ^d, scalar α, token t, the perturbation x ← x + αv̂ shifts the
t-logit by **Δℓ_t = α·(W_U[t,:]·v̂) = α‖W_U[t,:]‖₂·cos(v̂, ŵ_t)**,
ŵ_t = W_U[t,:]/‖W_U[t,:]‖₂.
*Proof:* Δℓ = W_U(αv̂); take the t-th row. ∎

Corollary (licenses the reconstruction): b_i = α_run·normalize(E_run[t_i] −
E_run[f_i]) (LOG-197 E1/E2, Q1=Supported [FACT]); the direct readout component
of the bridge's effect is Δℓ_{t_i} = α_run‖W_U[t_i]‖₂cos(b̂_i, ŵ_{t_i}), which
would satisfy ≥ 0.9α_run‖W_U[t_i]‖₂ had (a) fired. It did not.
License grade: IN-HOUSE-PROOF for the geometric endpoint (a); the causal upshot
(tilt geometry ⇒ the rescue was *caused* by readout tilt) remains
CONJECTURE-UNDER-TEST — moot for the exoneration direction, since the geometry
falsified the tilt's operationalization. Assumption A2 (mid-network propagation)
is undischarged and remains K2's question; the (a) endpoint did not need it.

---

## 8. Pre-registered consequences (§9) — K1-EXONERATED branch

Per the frozen plan, K1-EXONERATED (tilt Not supported) licenses: the CEO
revisits the LOG-204 §H7 demotion (it does **not** lock in); the K2 pilot is
licensed (GPU minutes); Sprint-3 Stage-0 pilots are licensed (positive class
intact); K3's CPU compliant-bridge construction audit proceeds; §H6 ordering
resumes. No pivot fires. EXP080/EXP081 remain gated on the Law #7
bridge-leakage audit + CEO GPU clearance (unchanged by K1).

---

## 9. FACT / INFERENCE / HYPOTHESIS / SPECULATION ledger

- [FACT] k = 0/60 items ≥ 0.9 on all three primary runs; CP 95% CI
  [0.0000, 0.0596]; (a)-RULED-OUT on EXP065, EXP066, EXP077-official.
- [FACT] Per-run verdicts: Not supported × 3; EXP070 Underdetermined;
  program-level K1-EXONERATED. All guards passed; forward passes = 0.
- [FACT] D1: L1-predicted logit-shift ratios match archived end-to-end ratios
  within 11% / 4.5% (pre-registered "strengthens" reading).
- [FACT] (b, c) recomputed from per-item records match archives exactly
  (EXP066: 8/0; smoke: 14/0); (c2) identity holds exactly.
- [FACT] Δθ=0 verified per-archive with runner-verbatim hash functions;
  A8 minimum = 0.784649.
- [INFERENCE] The bridge is a relational (t−f) direction whose rescue is
  readout-path-mediated (D1), not a target-row boost ((a)). Tilt exonerated;
  the autonomous output-side mechanism reading is not killed by K1.
- [HYPOTHESIS] The EXP077 smoke-vs-official (b, c) discrepancy (14/0 vs 6/0)
  may be partly explained by the discovered bench difference; the official
  bench assumption (frozen port) is unchallenged but now carries a noted
  uncertainty — the official per-item records were never mirrored in-repo
  (standing TODO from LOG-197).
- [SPECULATION] None licensed by this execution; none offered.

---

## 10. Open items for the CEO / Law #14

1. The G3 hash-function correction and the G2 smoke-bench scoping are disclosed
   deviations from the plan's letter, both flagged for Law #14 review. Neither
   touches a verdict cell.
2. The smoke bench anomaly (entity-grouped 6-per-entity vs the ported
   construction) is a new archive fact the program should disposition: it does
   not affect K1's verdicts, but any future use of the smoke per-item records
   must account for the unknown (target, foil) pairs.
3. Per §9, the §H7 revisit and K2/Sprint-3/K3 licensing decisions are the CEO's.

*Executor: K1 implementation track (LOG-213) · 2026-09-23 · $0 CPU, 0 forward
passes, GPU dark throughout.*
