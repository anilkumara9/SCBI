# CEO Diary — SCBI Research Program

The CEO's living record of the team: who did what, what the CEO told them, how the team's thinking is evolving, what's blocked, and what decisions were made. Agents are ephemeral; this file is the team's institutional memory. Every agent dispatch and every review verdict gets a line here.

---

## Entry 2026-09-23 (night shift) — The infrastructure night

**Roster tonight:** Mathematics Auditor, Corrections Integrator ×2, Adversarial Reviewer ×2, Deep Research agent, Innovation Synthesizer, Proposal Fixes Integrator (in flight), CEO.

**Standup — what each role delivered and the CEO's feedback:**

- **Mathematics Auditor** — Audited the Procrustes-failure proof (lemma PASS; caught a FALSE claim in our own proof doc's self-review; conditional scramble proposition PASS with labeled assumption) and verified 4 statistics independently (McNemar, winner's curse, ρ-gate floor). *CEO feedback: exemplary. This is the single highest-leverage hire of the night. The catch of our own false claim is the culture working — record it as the M2.5 precedent, don't bury it.*
- **Corrections Integrator (proof doc)** — 3 surgical fixes applied; verified EXP067 spec needed no change; LOG-087 (first). *CEO feedback: clean surgical work. The explicit "original claim was false" labeling is exactly right — shame-free correction is a team value.*
- **Corrections Integrator (EXP068 ρ-gate)** — Pinned B=9,999/seed 68026; pre-registered degenerate-gate rule; exhaustive outcome partition; LOG-091 (renumbered from a duplicate LOG-087). *CEO feedback: strong. The exhaustive partition table is the kind of artifact that prevents 3am execution bugs.*
- **Adversarial Reviewer (EXP068)** — Full-spec read, independent arithmetic recomputation, VERDICT: SIGN. *CEO feedback: the standard to hold. "Verified by full spec read, not on trust" is the sentence every review should end with.*
- **Deep Research agent** — The Sept 2026 field sweep: SVF, A-LQR, steering unreliability, Gemma Scope 2, conditionality-as-baseline. *CEO feedback: direction-changing work. The honest confidence qualifiers are what make it trustworthy.*
- **Innovation Synthesizer** — 7 proposals, all conjectural, all with kill criteria and free-tier costs. *CEO feedback: the N2-candidacy judgment for P1 was sound and survived review — but narrower than first written. Good ideas get sharper under pressure, not killed by it.*
- **Adversarial Reviewer (proposals)** — 7 accepts (1 clean, 6 conditional), 0 rejects. Caught the overclaim on P1's "first demonstration" and demanded a real non-inferiority margin. *CEO feedback: this is the job. The narrowed P1 is stronger than the original.*
- **Proposal Fixes Integrator** — Applying the 6 conditional fixes now.

**Mindset assessment (evidence, not vibes):**
- Falsification culture: WORKING. We corrected our own false proof claim on the record; we narrowed our own N2 claim under review; kill criteria have teeth (P6's is the strongest).
- Math standards: WORKING. Charter enacted; space checks binding; conditional results carry their conditions.
- $0-budget discipline: WORKING. EXP070 sequenced before EXP068's bundle; P3's $0 analysis before any execution.
- Growth edge #1: **zero GPU data so far.** All of this is paper until the user runs EXP067/EXP070. A team that pre-registers beautifully but never executes is a reading club.
- Growth edge #2: **Gemma-3 literacy.** The field moved; our tooling knowledge is Pythia-era. P4's port plan must close this gap.
- Growth edge #3: **concurrent bookkeeping.** Two agents wrote duplicate log numbers tonight (LOG-083 ×2, LOG-087 ×2). Fixed and logged as LOG-093. New rule: the CEO assigns log numbers before dispatching agents that write to the log.

**CEO decisions tonight:**
1. EXP068's execution bundle is CLEARED but NOT started — sequencing holds behind EXP070's verdict. Discipline over eagerness.
2. P6 (cone-vs-line) preregisters early — cheapest mechanism test in the set.
3. P3 ($0 anti-steerability analysis) runs before any EXP068/070 execution.
4. P7 stays parked until the host-model question is answered.
5. Static CAA is demoted from method-under-test to ablation baseline, effective immediately.
6. Daily CEO health check cron installed (morning, user timezone): repo integrity, log freshness, experiment status, blockers. Reports only what needs attention.

**Blocked on:** the user's GPU runs — EXP067 (Pythia-410m, Kaggle runbook) and EXP070 (oracle ceiling, runs first strategically). Nothing else on the critical path can move without these.

**Open problems the team is carrying:** probe transferability to per-instance views (assumed, unvalidated); constant-S_E measure-zero edge (named, unruled); §4b probe's unpinned permutation p (flagged for probe-code time); Gemma port feasibility; whether the SVF head-to-head's non-inferiority margin is achievable on a T4.

**Diary note 2026-09-23 (close of shift):** The 6 conditional proposal fixes are applied and the sprint is finalized — 15 ideas, 7 accepted with conditions, sequencing recorded (P6 preregisters early; P3's $0 analysis before any EXP068/070 execution; P7 parked on the host-model question). Process learning, recorded honestly: the CEO's own dispatch of the fixes integrator carried no pre-assigned log number — the discipline I enacted this morning. The integrator took the next free number (LOG-094) correctly on its own, so no collision occurred, but the miss is mine. The rule only works if the CEO follows it first. Going forward: every dispatch brief opens with the reserved log number.

**Diary note 2026-09-23 (Kaggle packaging):** Built ready-to-run Kaggle packages for both pending GPU experiments — notebook + bundle zip + setup guide in `~/workspace/your_files/kaggle/` (EXP070 oracle ceiling, EXP067 QK/OV subspace). Notebooks encode the runbooks as executable cells with pre-flight GPU checks, bundle self-tests, and artifact packaging. Sent to the user; ball is in their court for the actual runs.

**Diary note 2026-09-23 (no-GPU workstreams):** User asked whether any alternative exists that needs no help from them — honest answer: no free GPU is obtainable without a human-verified account, and the protocols forbid CPU execution by design. Instead of idling, dispatched two CPU-side workstreams: (1) P6 cone-vs-line pre-registration draft (cheapest mechanism test, clean accept — draft marked NOT SIGNED, goes to adversarial review on return); (2) EXP075 execution bundle build (runner/evaluator/tests/runbook, validated locally, NOT CLEARED for execution until bundle review). Both carry log-number discipline. GPU runs (EXP067/EXP070) remain the critical path and still need the user.

**Diary note 2026-09-23 (execution track unblocked):** User agreed to the split — they set up the Kaggle account (incl. phone verification, which unlocks the free GPU quota); the team runs everything after that. No Kaggle skill exists in the catalog, so execution goes through the live-browser route: secure sign-in card (request_login) once verified, then a browser task drives the full KAGGLE_SETUP.md flow (notebook from .ipynb, T4×2, Internet ON, run, monitor, download, evaluate). Dispatched P3's $0 analysis now so the accepted sequencing (P3 before EXP068/070 execution) is honored rather than waived.

**Diary note 2026-09-23 (EXP077 draft → review):** P6 preregistration draft landed as EXP077 (LOG-095), bannered DRAFT — NOT SIGNED. Drafter self-flagged two weaknesses: (1) kill branch rests on single [ARBITRARY] ρ=30° (radius misspecification risk); (2) (a)(ii) radial trigger carries ~19% family-wise FPR across four α tests without multiplicity correction. Dispatched Law #14 adversarial review with explicit instruction to attack those two first, then the standard battery. No signing until review lands.

**Diary note 2026-09-23 (second collision, my miss):** P3 agent and EXP077 agent both claimed LOG-095 — I dispatched P3 without pre-assigning a number, repeating the exact mistake the rule was written to prevent. Repaired: P3 → LOG-097, repair → LOG-098. Lesson upgraded from "pre-assign before dispatch" to "the number goes IN the dispatch brief; agents never choose." P3 headline: anti-steerable fraction 0.0% everywhere → degenerate branch → no amendment to signed EXP068/070, sequencing gate satisfied (analysis review remains, not GPU work). EXP065 per-instance ledger confirmed missing (Law #13 lesson); coherence diagnostics uncomputable (vectors never archived — open).

**Diary note 2026-09-23 (P3 closed, deliverable repaired):** Review verdict ENDORSE-WITH-QUALIFICATIONS; reviewer independently recomputed every number from primary artifacts — all match; tautology charge ruled to fail (operationalization was pre-registered). Deliverable arrived truncated (Q3 cut mid-sentence, Q4 missing); completed from the reviewer's own verdict summary with an explicit completion note. Q1–Q4 applied as Addendum A on the analysis (original untouched). P3/EXP070 sequencing conflict formally resolved: mandate discharged, EXP070 may proceed on its signed protocol. Standing note: agents' long file writes can truncate — future review briefs should require the agent to verify its deliverable's tail before reporting done.

**Diary note 2026-09-23 (EXP077 review: SIGN-WITH-FIXES):** Reviewer found a genuine logic bug — missing C3-replication branch meant a boundary-null replication failure would have been misread as cone evidence (the most load-bearing MAJOR). Also upheld radius misspecification (ρ=30° must be baked into the kill license, not parked in DOES-NOT) and multiplicity (Holm-adjusted p-values; headroom floor becomes 8 net items). Fix agent dispatched with pre-assigned LOG-103 to apply all 10 fixes; re-verification will follow before any signing. Third log collision (LOG-097×2) repaired → LOG-102; the pre-assignment rule held for the fix dispatch.

**Diary note 2026-09-23 (EXP075 bundle review: CLEAR-WITH-FIXES):** Reviewer's most load-bearing catch: the evaluator routed the exact-kill cell (C4 b=c=0, C3 valid) into branch (i), making the pre-registered kill criterion UNREACHABLE in code — RULINGS["f"] was dead code, silently weakening falsification (Law #4). Second MAJOR: the documented multi-token early-abort didn't exist in code (silent token[0] truncation) — a docs falsehood. Also corrected: true budget ≈1,082 (spec's 420 forgot support forwards), and the "no historical EXP066 script" caveat was false — reviewer diffed and found item-identical constructions (provenance upgraded). Fix agent dispatched (LOG-105 pre-assigned); focused diff re-verification follows before any clearance. Pattern worth noting: reviewers keep catching kill-criterion-weakening bugs (EXP077's missing replication branch, EXP075's dead kill branch) — the adversarial layer is the highest-ROI part of this pipeline.

**Diary note 2026-09-23 (EXP077 fixes → re-verification):** All 10 fixes applied (LOG-103). Fixer flagged a sharp asymmetry question for the re-verifier: (a) CONE-WINS generalizes from a ρ=30° positive while (c)'s kill is now radius-bound — legitimate falsification asymmetry (existence generalizes, universal kills don't) or a symmetry violation needing the same qualifier? Re-verifier dispatched (LOG-106 pre-assigned) with explicit instruction to rule on it; SIGN or BACK-TO-FIXES. If SIGN, the draft becomes the program's third signed pre-registration of the day.

**Diary note 2026-09-23 (EXP077 SIGNED):** Re-verifier verdict SIGN (LOG-106). All 10 fixes verified; the asymmetry question ruled legitimate — existential claims generalize from an instance while universal kills need in-sentence bounds; forcing "at ρ=30°" into (a)'s license would misleadingly imply confinement. One-line licensed reading recorded. EXP077 is now the program's newest signed pre-registration, awaiting GPU execution. Program state: signed-but-unexecuted protocols EXP067, EXP068 (bundle pending), EXP070 (bundle built), EXP075 (bundle in fixes), EXP077 (prereg only, bundle not yet built). Every GPU run awaits the user's Kaggle.

**Diary note 2026-09-23 (EXP075 fixes → diff re-verification):** All 9 fixes applied (LOG-105), test suite 25/25 green (was 16 — 9 new tests added). F1 verified in the fix report: kill cell → terminal "f", RULINGS["f"] live. Diff re-verifier dispatched (LOG-107 pre-assigned) as the execution-clearance gate, with explicit instruction to rule on the fixer's F9 mechanism question (accept behaviorally-identical implementation vs. require the literal simpler wrap — instructed not to fail on style alone). If CLEAR, EXP075 joins EXP070 as execution-ready on Kaggle.

**Diary note 2026-09-23 (EXP075 CLEARED FOR EXECUTION):** Diff re-verifier verdict CLEAR (LOG-107); independently ran the suite — 25/25 PASS. F9 mechanism accepted (behaviorally identical, not failed on style). Bundle banner updated to CLEARED FOR EXECUTION. Program state: TWO experiments execution-ready (EXP070 oracle-ceiling, EXP075 subspace bridge), EXP077 signed (bundle not yet built), EXP068 signed (bundle pending EXP070), EXP067 signed (bundle built). All await the user's Kaggle sign-in + phone-verified GPU quota. The sign-in card is with the user; no further CPU-side work can advance execution.

**Diary note 2026-09-23 (first GPU run crashed; hotfix applied):** EXP070 died 33s in on an UnboundLocalError — the entity variable `D` shadowed the dimension `D` in main(). A humbling catch: our whole review apparatus (protocol review, bundle review, diff re-verification) never *ran* the runner — py_compile can't catch a runtime scoping bug, and the evaluator tests don't touch the runner. The browser operator did exactly the right thing stopping without editing. Fixed both runners (EXP075 had the identical latent crash — would have died the same way), AST-audited for siblings, rebuilt both Kaggle zips, logged LOG-108. New standing rule: no bundle clears for execution without a startup smoke test. Re-running EXP070 now; ~26h of quota remains, 33 seconds spent — negligible cost, valuable lesson.

**Diary note 2026-09-23 (second startup crash; dtype root-caused):** fp16 model (new transformers honors config torch_dtype) vs float32-assuming script. The killer evidence: EXP066's script has the identical randn+dot pattern and never crashed — history ran fp32. One-line pin (torch_dtype=torch.float32) restores both the script's intent and historical parity. Applied to both runners, zips rebuilt, LOG-109. Two startup crashes in a row is a process failure, not bad luck: I'm now installing CPU torch locally to smoke-test the runner past every crash point BEFORE it touches the user's GPU quota again. The quota is the program's scarcest resource; my embarrassment is not the point — not burning their hours is.

---

## Entry 2026-09-23 (morning) — Research Lead takes the desk

**Roster change:** Research Lead hired and briefed (LOG-132, CEO-authored entry). Chain: User → CEO → Research Lead → specialists. Adversarial verdicts still answer to the CEO; the Lead never clears GPU or rules on design flags.

**Thread states (Lead's working picture):**
- **Sprint-2 COMPLETE.** Report: research/innovation/SPRINT2_2026-09-23.md. Output review (Lead verdict): ACCEPT the sequence with two conditions — (a) G1 runs under a frozen mini analysis plan written before touching weights (endpoint, null, thresholds fixed — the P3 precedent: the analysis is only load-bearing if the operationalization is pre-registered); (b) C-A's pre-registration must survive a dedicated adversarial review of the entity-similarity-leak confound (bridge ≈ logit steering) before any bundle work. Strengths noted: tiered cheap-first gating, honest kill list, forced baselines named (debate/MoA at equal FLOPs, STARS, DEER, ∇-Reasoner, bigger-frozen-model at matched compute), corpus challenges recorded — the §3.3 "theorem" relabeled [CONJECTURE] is a genuine catch by the theory agent. All candidates N1, ledger grep-checked, 0 corpses re-proposed.
- **EXP079 — BLOCKED on two fronts.** (1) The builder's final report never arrived (interim only: files written, 27/27 tests, smoke was running ~300 forwards; no zip, no hashes, no Law #14 review). (2) Design flag unresolved: interim construction found N_final=26<50, so the signed HALT_PROBE readiness gate would fire by design. Adjudication options for the CEO: (i) accept the registered halt — run the readiness computation only (CPU-cheap, no GPU), log it as a reportable readiness result, close EXP079, assign a new number for any redesigned probe mechanism; (ii) rule the gate miscalibrated and order a re-registration under a new experiment number with a redesigned probe-yield mechanism — NOT a silent relaxation of the signed gate (Law #4). The Lead does not rule; the builder's final report is still the missing evidence.
- **EXP068 — unlicensed, survives.** Still needs EXP079's ruling (informative probe + no C3-vs-C2 gain → cancel current form; large oracle win → justify build; still-uninformative → survives, next step is pool-diversity diagnostic). No bundle work until licensed.
- **Paper draft (reports/paper_draft.md) — Phase 1 sign-off complete, adversarial review SIGNED, ~690 lines.** Next step: the boundary narrative now has two new licensed results to absorb — EXP077's flat-zero (cone/line/offset/radial all dead, bridge rescues +10pp) and EXP067's Stage-A halt (rank 33/64 — the "sound" Procrustes underdetermined). Both strengthen the paper's central claim; EXP067's halt also constrains the mechanism section (see Lead's challenge below). Then: user decision on venue/timing (workshop vs main track) — reserved gate.
- **Closed threads:** EXP077 (c) NEITHER flat-zero; EXP067 (a) STAGE_A_HALT; EXP070 (c2) UNINFORMATIVE_PROBE; EXP078 (a) ENERGY_GATE_HALT; EXP075 aborted (F2 multi-token). Static-geometry program: 0-for-everything; bridge rescues everywhere it appears.
- **Bookkeeping flag for CEO ruling:** LOG-132 collision — the sprint-2 coordinator's report entry and the CEO's Research-Lead-hiring entry both carry LOG-132 (the sprint dispatch carried no pre-assigned number). The Lead did NOT repair this (charter: the Lead never mints numbers). Recommended repair: renumber the sprint-2 report entry to LOG-133, keeping the CEO-authored hiring entry at LOG-132.

**Ranked idea backlog (skeleton, from sprint-2 + live threads):**
1. **G1** — weight-only QK/OV projection-energy audit. Kill: e_QK(B_agg) ≥ e_QK(bridge) → null-space mechanism dead, Phase 2 re-routed before any more QK-GPU spend. Cost: $0 GPU, CPU minutes. N1. [READY TO DISPATCH — awaiting LOG number from CEO]
2. **G2** — oracle ceiling over output-side pool, Phase A only. 1,140 passes ≈ 52s. Kill: oracle-vs-static ΔM ≤ 0 → output-side evaluator program stood down. Gates all evaluator-shaped work.
3. **C-A** — cross-item donor-bridge transfer (best label-free capability shot). 2,700 passes ≈ 2 min. Kill: ΔM ≤ 0 while oracle self-bridge rescues. Needs pre-reg (next free number EXP080 at pre-reg time) + adversarial review of the similarity-leak confound first.
4. **C-B** — self-bridge fixed-point iteration, target-free ≤5 passes/item. 2,880 passes ≈ 2.2 min. Informative null (c>b proves bridge power is label-derived).
5. **C-C** — bridge-amplified CoT, ceiling-first (label-informed ceiling must survive before target-free).
6. **M1/M2/M3** — mechanism probes; M3 sequenced behind G1's verdict.
7. **Foundations track** — template-contamination + anisotropy analyses, P3-style, CPU-first (boundary-paper hygiene).
8. **Paper draft update** — absorb EXP077/EXP067, then venue decision.
9. **EXP079** — builder report + design-flag ruling (CEO).
10. **EXP068** — licensing decision downstream of EXP079.

**Proposed weekly schedule:** Wednesday adversarial slot — first up G1's frozen analysis plan (once drafted), then C-A's pre-registration draft (confound review). Friday cull — standing items: sprint-2's kill list (done, recorded), EXP079's design-flag outcome, any idea the night's nulls rule out. Monday ideas — seed from corpus challenges (the §3.3 challenge, RQ-BASIS-001, Lead's idea below).

**Lead's knowledge-protocol obligation (one challenge + one idea):**
- **Challenge:** the boundary paper's mechanistic explanation (null-space downstream filtering) currently rests on zero direct measurements. EXP078's energy number (e_median=0.0544) is *consistent* with null-space filtering but cannot distinguish it from "the bridge is simply a different direction family with no shared geometry" — and the injected v̂ basis in EXP065/066 was the scrambled one, so ΔM=0 can't separate "nulled downstream" from "pointed nowhere" (the theory agent's confound point, independently confirmed). If G1 kills the null-space mechanism, the paper's mechanism section must be rewritten to readout-misalignment-or-unknown — the boundary *claim* survives (it rests on the endpoints), but the *explanation* does not. G1 is load-bearing for the paper, not just Phase 2.
- **Idea:** J-lens-pruned adaptive search. The sweep found nobody has combined J-lens identification (Anthropic, Jul 2026) with an adaptive steering loop. Mechanism: run G1 to measure *where* the bridge's projection energy lands (which downstream heads' QK subspaces); restrict EXP068's G/E/S/T per-instance search to those head-subspaces instead of the dead v̂ geometry — a search space pruned by measurement. Math: candidate set C = {P_{QK,h}(bridge)/‖·‖ : h ∈ top-k heads by e_QK(bridge)}; loop searches over span(C) per item. Kill: head-restricted loop fails to beat static full bridge (ΔM ≤ 0, McNemar p ≥ 0.05) → the loop adds nothing over the bridge. Cheapest: G1 first (minutes, $0), then 60-item pilot ≈ 2 min GPU. Novelty: N1 (J-lens exists; combination untested per sweep). Roadmap: Phase 4 primitive. Status: [CONJECTURE], parked behind G1.

**Diary note 2026-09-23 (Research Lead — G1 + EXP079 recovery dispatched):** Executed the CEO's LOG-135 rulings. (1) G1 dispatched under LOG-134 with binding condition (a): the specialist writes the frozen mini analysis plan (research/analysis_plans/G1_PLAN.md — exact e_QK/OV endpoint, head/layer set, vector provenance on disk, random-r null with pinned seed, exact permutation p, thresholds, kill e_QK(B_agg) ≥ e_QK(bridge)) BEFORE touching weights, then PAUSES for my go-ahead; the plan goes to the Wednesday adversarial slot, and only then does Phase 2 execute. CPU-only, local venv, torch.float32 pinned, read-only cached weights, no Kaggle. The specialist owes the knowledge protocol's challenge + idea. (2) EXP079 recovery dispatched under LOG-136: verify true bundle state (no zip exists; smoke completion unknown; interim claimed 27/27 tests and N_final=26), recompute N_final from code/data (not on trust), audit the 11/25 multi-token finding — then either complete the build (tests, smoke, zip, hashes, drafted LOG-122 entry) or report unrecoverable and rebuild from the signed spec. Design untouched: the N_final ≥ 50 gate stands; the halt-by-design flag goes to the CEO only after build state is verified. Routine stands: Wednesday slot takes G1's plan (then C-A's pre-reg draft with the entity-similarity-leak confound front and center); Friday cull takes the sprint-2 kill list, EXP079's design-flag outcome, and anything the nulls rule out.

**Diary note 2026-09-23 (Research Lead — G1 Phase 1 landed; Wednesday review dispatched):** The G1 specialist delivered its frozen plan (research/analysis_plans/G1_PLAN.md, 273 lines) under LOG-134 and paused per condition (a) — no weights touched. Lead's assessment: the plan is strong — full math-charter formalization (M1–M8), exhaustive KILL/CONSISTENT/INCONCLUSIVE partition, Phipson–Smyth exact permutation p, pre-registered attack surfaces (bridge-reconstruction fidelity, layer sensitivity, mean-vs-max, anisotropy via relative-to-null percentiles). Two genuine contributions: (i) vector provenance better than expected — EXP077's archive holds B_agg, the five v̂_k, B_wrong, B_⊥ (⊥ to 1e-8), cone/control families, matching pre/post hashes; only "mean bridge(x)" needs weight-only reconstruction; (ii) a proved [FACT] that P_QK is exactly invariant to rotary embeddings (left-multiplication by invertible R preserves row space) — neutralizes the strongest anticipated objection to a weight-only QK audit. The specialist also added a companion diagnostic the sprint didn't specify (20 random-token-pair steering directions s_j with a pre-registered rejection rule for the "maximize QK energy" design objective) — motivated by its own challenge: even a CONSISTENT verdict must not upgrade the bridge's mechanism story, since the bridge's rescue is independently explained by readout bypass. Dispatched the Wednesday adversarial review under LOG-137 with seven ordered attack points (endpoint formalization, rotary proof verification, partition exhaustiveness, bridge-reconstruction fidelity vs the runner's make_bridge_vec, vector provenance spot-check, ruling on the two judgment calls, head/layer-set completeness). Reviewer's verdict answers to the CEO; the Lead will not soften it. If CLEAR, the Lead issues the Phase 2 go-ahead. EXP079 recovery (LOG-136) still running.

**Diary note 2026-09-23 (Research Lead — EXP079 recovery landed; Law #14 review dispatched):** The LOG-136 recovery agent delivered a complete standup: bundle COMPLETE (no rebuild needed; 24/24 evaluator checks green — the interim's "27/27" was not reproducible; zip 05cfad2c… built; stale pycache removed). Three verified facts: (1) N_final=26 recomputed from the runner's own build_probe_map — the even/odd split deterministically starves five triple patterns of HELDOUT data (probe-size distribution {0:16, 5:18, 10:26}); the signed HALT_PROBE gate fires by design. (2) The runner refuses CPU by design (no --allow-cpu; adding one = design change, not made) and loads the full model (~2 min of user quota) before the deterministic metadata gate fires — a GPU launch would burn quota to rediscover a CPU-proven halt. (3) The CPU readiness harness replicates the exact gate path; the real evaluator binary rules branch (a) READINESS_HEADROOM_HALT verbatim. The agent also discharged a CHALLENGE (the "multi-token names are harmless / unaffected by construction" claim in UNTESTED_ASSUMPTIONS R6 is [CONJECTURE] cited as [FACT]; falsifier: first-token vs full-sequence probe margins on the multi-token subset) and an IDEA (the shortfall is a split problem — a future new-number pre-registration could use per-(hop,pattern) stratified 50/50 split, with a CPU-free kill criterion: N_final≥50 proceeds to pre-reg, N_final<50 proves the 5-vocab family structurally too small for any out-of-sample 15-item probe). LOG-122 (build entry, agent-drafted) and LOG-136 (recovery) appended to the log. Dispatched the Law #14 review under LOG-138 with the precise question: (a) is the bundle faithful to the signed spec, and (b) is accepting the registered halt via the CPU readiness computation — no GPU launch — legitimate under the protocol? Reviewer's verdict answers to the CEO. Lead's recommendation to the CEO: (i) accept the registered halt via the CPU readiness computation and close EXP079; (ii) remains open for a redesigned probe under a new number, gated first by the stratified-split CPU test. EXP068's licensing still awaits EXP079's closure.

**Diary note 2026-09-23 (Research Lead — G1 review verdict in; Phase 2 go-ahead issued):** LOG-137 landed: **CLEAR-WITH-FIXES** — no FATAL, no MAJOR; five MINOR mechanical fixes (F1: bridge rescue number corrected to the archived run's +23.33pp/b=14/c=0/p=0.000122; F2: anisotropy limitation disclosed; F3: s_j exclusion at token-id level + aggregation-matched s̄ pre-registered; F4: rotary [FACT] strengthened with block-diagonal line; F5: snapshot cache-path provenance recorded), none requiring re-verification. Reviewer verified: endpoint formalization correct, rotary proof step-by-step, decision partition unique and exhaustive, bridge reconstruction matches make_bridge_vec exactly (the highest-risk surface — clean), vector provenance spot-checks pass (all norms 1.0, ⊥ to 1e-8, hashes match), s_j companion KEPT, OV secondary-only KEPT, head set complete (all 48 downstream heads). Reviewer's standing note for the G1 report: foreground per-head energy profiles (the parked J-lens-pruned search depends on that table). Lead applied F1–F5 to research/analysis_plans/G1_PLAN.md, re-frozen (weights still untouched), appended LOG-137 to the research log, and issued the Phase 2 go-ahead to the G1 specialist — execution now running (CPU-only, float32, read-only weights, zero forwards). LOG-138 (EXP079 halt review) still running. When G1's verdict lands, the Lead verifies kill/survive and reports up; the paper's mechanism section rewrite hinges on it.

**Diary note 2026-09-23 (Research Lead — LOG-138 in; EXP079 halt review-backed, CEO adjudication requested):** The Law #14 review returned **ACCEPT-WITH-FIXES**: the registered HALT_PROBE halt is accepted via the CPU readiness computation; no GPU launch is scientifically required or justified. All five LOG-136 claims independently confirmed by recomputation (N_final=26, three disjointness assertions, 11/25 multi-token audit, CPU gate path → branch (a) READINESS_HEADROOM_HALT verbatim through the real evaluator, spec fidelity on the halt path). Four findings, all latent downstream of the deterministic gate: F1 (doc: "27/27" → 24/24, corrected on the record), F2 (C4 seed 4242 vs spec-pinned 7004, silent), F3 (runner implements 5 of 7 conditions — C5/C6 absent), F4 (MAJOR-if-reached: probe-signal gate coded as conjunction vs the spec's two-path structure — decision-flipping in a completed run, unreachable here; repair + re-verification mandatory before any future reuse past the gate). The reviewer's structural result is the night's sharpest process lesson: max N_final = 46 < 50 over ALL possible 75/75 splits — the halt was the only possible outcome, infeasible by construction; the LOG-120 pre-reg review never computed N_final. It also falsifies the recovery agent's stratified-split idea (would give N_final=0). The reviewer's process idea — probe-feasibility algebra as a standing pre-registration gate (closed-form max-N_final before signing) — is recommended for adoption. LOG-138 appended to the research log. **CEO adjudication requested:** (i) accept the registered halt via the CPU readiness computation and close EXP079 — the reviewed basis is complete (LOG-136 + LOG-138); (ii) remains open for a redesigned probe under a new number, with the binding constraint now known to be (key × floor × support-size), not the split mechanism. Registered consequence for EXP068: the halt leaves the ceiling unmeasured → EXP068 survives; next step per the signed licensing is a pool-diversity diagnostic, not a third probe iteration. G1 Phase 2 execution still running — its verdict is the remaining load-bearing number for the paper's mechanism section.

**Diary note 2026-09-23 (Research Lead — mentorship-directive implementation, CEO orders LOG-140–148):**

**LOG-140 — tracks staffed, synthesis commissioned.** Wrote `research/EXPERT_TRACKS.md`: all 12 tracks mapped (1 theory, 2 G1-lineage, 4 literature, 7 Law #14, 8 evaluator-design lineage now FORMALIZED as experimental statistician, 10 innovation sprints; newly staffed: 3 neuroscience/cognitive science, 5 superintelligence/AGI architecture [explicit], 6 systems, 9 benchmark scientist, 11 scaling, 12 scientific discovery). Wrote the binding synthesis spec `research/synthesis/SYNTHESIS_SPEC.md` (evidence base with exact numbers, P1–P4, deliverable form the mentor expects, H1–H5 boxed questions, G/H gate trigger). Dispatched three track-cluster agents: A foundations (1,2,5,7 → §§A–C,E-input,I-input), B evidence & validity (3,4,8,12 → audit,B,D,discovery criterion), C futures (6,9,10,11 → §§E–H,J). All under knowledge-protocol reading + double-labeling + verdict-standard discipline. Next: synthesizer dispatch once cluster reports land (research/synthesis/CLUSTER_{A,B,C}_2026-09-23.md), then Law #14 review before program adoption.

**LOG-141 — epistemic reporting layer adopted program-wide.** `research/TEAM_KNOWLEDGE_PROTOCOL.md` §3: FACT/INFERENCE/HYPOTHESIS/SPECULATION is now the canonical reporting layer on top of the repo 10-label standard, with the exact mapping; both layers required on load-bearing claims. The mentorship directive added to the mandatory reading list.

**LOG-142 — canonical objective adopted.** `research/RESEARCH_OPERATING_SYSTEM.md` §1: θ_after=θ_before with temporary state (B_t,z_t,C_t,M_t,…) evolving; organizing question stated; P1 mechanism-level bar as standing law. `theory/README_FORMULATION.md` §0: the formal objective, with explicit note that anchored definitions are UNCHANGED (Law #5 Definition Change Protocol not triggered — doc update, no definition change; no signed protocol content touched).

**LOG-143/144/145 — live gates, no restart.** (143) G2 pre-reg DRAFT dispatched: oracle ceiling over output-side pool, Phase A only (1,140 passes ≈52s); kill = oracle-vs-static ΔM ≤ 0 → output-side evaluator program stood down; Phase B deferred behind ≥+12pp win + new pre-reg + EXP068 reconciliation; Law #14 review required before PRE-REGISTERED; no experiment number minted (assigned at signing, next free EXP080). (144) C-A entity-similarity-leak adversarial review dispatched FIRST, before any C-A pre-reg draft: reviewer formalizes the confound (donor bridge ≈ entity-similarity/logit steering vs genuine relational transfer), mandates the design controls the pre-reg must contain, sets SIGN/REJECT criteria, and rules PROCEED/REVISE/DO-NOT-RUN. Verdict answers to the CEO. (145) Paper mechanism rewrite dispatched: readout-misalignment-or-unknown per G1 kill, EXP077/067 absorbed, every number re-verified against artifacts, revision note in the paper (retraction culture — old wording preserved, not silently edited).

**LOG-146 — program culture note (mentor's behavioral note, adopted).** Killing attractive hypotheses is the behavior to preserve — it outranks accumulating positives. G1 (the §3.3 null-space sentence, killed not rescued) and EXP079 (the infeasible probe rule, closed not relaxed) are the reference examples. This is now the diary's standing culture line; the weekly mindset review will ask whether we lived it.

**LOG-147 — standing law against theory preservation adopted.** `research/RESEARCH_OPERATING_SYSTEM.md` §1: "No hypothesis earns continuation because it is elegant, ambitious, or already heavily implemented. It earns continuation only because its discriminating experiments survive pre-registered attempts to falsify it." Theory preservation named as the program's most dangerous failure mode; enforced in triage (elegance/ambition/investment-justified proposals get challenged or culled). Three evidentiary levels are standing law: "can improve inference" ≠ "changes the computational strategy" ≠ "creates qualitatively new capability" — every report states its level, no silent crossing.

**LOG-148 — evidentiary verdict standard applied.** Only permitted verdicts: Supported / Not supported / Inconclusive / Underdetermined / Refuted (exact definitions in the directive file). "Interesting/promising/mechanistically elegant/worth another experiment" are NOT verdicts — [INTERPRETATION] only, alongside a real verdict; writeups concluding with a banned phrase and no verdict are sent back. Applied to the synthesis spec (§10) and to the knowledge protocol's report template (§3). Governance setup from the mentor's messages is now complete; from here: staffed tracks, commissioned synthesis in the specified form, live gates running, evidence coming back.

**Diary note 2026-09-23 (Research Lead — LOG-149/150: C-A confound review verdict in, pre-reg drafter dispatched):** The LOG-144 adversarial review returned **REVISE** with nine binding (c) items the pre-reg must satisfy before it can be signed — no partial signatures. The review's sharpest contributions: (1) the centroid identity (donor bridge = donor token-prior direction) makes leakage the *null hypothesis*, not a corner case — the 10-token answer pool guarantees donor↔test overlap structurally; (2) the degeneracy trap — balancing donors annihilates the intervention itself, so the naive "fix" is worse than the confound; (3) the one-sided kill criterion is repaired with the conjunctive T1–T6 license + verbatim anti-creep sentence; (4) evidentiary cap at Level 1 for any C-A win (readout steering under both hypotheses). Dispatched the C-A pre-reg drafter under LOG-150 with all nine items binding; the same reviewer signs under LOG-144 authority once they are present. **Process recommendation for CEO ruling:** adopt the reviewer's $0-GPU weight-only leakage pre-audit (centroid degeneracy + per-item s_t/s_f) as a standing pre-registration gate for any donor-bridge design — it generalizes the probe-feasibility-algebra discipline (LOG-138) and falsifies degenerate designs before a single forward pass. If C-A's leakage verdict fires (C5≈C3), Tier-1's lead capability bet passes to C-B per the sprint sequence.

**Diary note 2026-09-23 (Research Lead — LOG-151/152: G2 draft landed, Law #14 review dispatched):** The G2 pre-reg draft is in: 12-member output-side pool, 1,140 passes, feasibility proved in closed form (min=max N_final=60≥50 — the LOG-138 discipline held at draft time), M5.2-exhaustive tree with per-branch licenses, verdicts, and level-1 caps. Two design judgments for the reviewer's verdict: (a) B_⊥/B_wrong inside the oracle pool — the drafter's argument (fallback-to-control makes the oracle–static gap pure selection gain) is principled on its face but needs adversarial pressure; (b) Q_S pinned from the EXP078 smoke artifact with an (a)-halt on mismatch — the drafter itself flagged this as the likeliest review-fire target. The drafter's self-challenge (self-bridge dominance → "bridge ceiling, not pool ceiling") is the right kind of honesty and the §7 audit is the mechanism that keeps it honest. Dispatched the Law #14 review under LOG-152 with eight ordered attack points; PRE-REGISTERED only on SIGN. G2 remains the gate for all evaluator-shaped work.

**Diary note 2026-09-23 (Research Lead — LOG-153: Cluster A landed; Law #7 bridge challenge escalated):** Cluster A's report is in with a genuine surprise: the bridge — our most-cited positive and the foundation of the entire output-side pivot — may itself violate Law #7, since it is built from the target/foil option-token unembedding rows. If the audit bites, every "rescue" is a label-assisted readout artifact and the output-side successor dies with SCPM. This is the kill discipline working as designed: the program audited its own positive control against its own law. The cluster's verdicts are otherwise clean (I1 Supported, §3.3 Refuted, rescues Supported at level 1, loop Inconclusive). **Escalated to CEO:** rule whether the Law #7 bridge audit enters §H as a failure-experiment candidate. My recommendation: yes — H should contain the strongest available kill for the mechanism family, and a Law #7 violation of the positive control is exactly that. If adopted, the G2 design (which leans on the self-bridge as positive control and headroom gate) needs re-examination: a leaky positive control invalidates the headroom logic. Holding G2's review (LOG-152, running) unaffected for now — the reviewer may independently reach the same point — but flagging the dependency.

**Diary note 2026-09-23 (Research Lead — LOG-154: Cluster C landed; twin bridge challenges):** Cluster C delivered the futures half: six boxed architectures, three discriminating experiments, the Novel-Task Discovery Protocol killer (3–5h T4/candidate — GPU clearance will be a CEO decision at pre-registration), and the failure experiment with its explicit kill license. The notable event is convergence: Cluster C independently challenged the bridge (readout tilt, label-shuffle falsification) from a different direction than Cluster A's Law #7 challenge (option-information leakage). Two independent clusters, same target — this is not idiosyncrasy, it's the evidence pointing. It strengthens the case for the pending CEO ruling on folding the bridge audit into §H. The synthesizer's reconciliation list is growing: (a) A's track-5 I-input vs C's provisional §J definition; (b) two H5 candidates C5/C6 — one or both; (c) the twin bridge challenges — which falsification goes into §H, and what a confirmed readout-bias verdict does to the bridge's positive-control status across EXP065/066/070/077 (C flagged this as undecided — correctly, since demoting a positive control in signed protocols is a CEO-level decision). Cluster B (evidence & validity) is the last one running; synthesizer dispatches on its handoff.

**Diary note 2026-09-23 (Research Lead — LOG-155/156: paper rewrite landed, collision repaired, re-review dispatched):** The paper rewrite is in: §7 retracts the null-space theorem on G1's KILL with the theorem quoted verbatim, I1 held on endpoints (Supported), the bridge mechanism recorded as Underdetermined with three honestly labeled hypotheses, EXP067's halt recorded, §12 revision note preserving all superseded wording verbatim — retraction culture done right. The agent re-verified every number against artifacts (Law #2) and, importantly, caught and labeled the EXP077 provenance split the program has tripped on before (GPU +10pp vs smoke +23.33pp). Two things needed repair and attention: (1) the agent self-logged under LOG-145, colliding with my dispatch entry — repaired to LOG-155 per the LOG-093 precedent, with the process fix (briefs now explicitly forbid agents writing their own log entries); (2) dispatched the Law #14 re-review (LOG-156) the paper's status line requires, with a specific charge on the §7.3 caveat — is it adequate given the twin bridge challenges, or must the paper say more? A SIGN clears the draft for external use; venue stays with the user.

**Diary note 2026-09-23 (Research Lead — LOG-157/158: Cluster B landed, synthesizer dispatched):** All three clusters are in. Cluster B's headline is the honest one the program needed: "tested mechanism is prior art; claimed mechanism is a conjecture in a field that has shipped the adjacent possible" — ∇-Reasoner (ICLR 2026, test-time gradient descent on logits) sits exactly at the output-side locus our evidence points to, and Activation-LQR (Apr 2026) does closed-loop frozen-model control with formal guarantees. The synthesis now has to earn its novelty boundary against opponents that actually exist. B also filed a live challenge with a falsifier: EXP023's "AUTONOMOUS SCBI CONFIRMED" (dev N=20 grid, unadjusted p=0.000488) goes to exploratory if adjusted p ≥ 0.05 — that audit should be scheduled. And B tagged Steering Vector Fields UNVERIFIED — the diary/sprint's retelling of SVF must not be cited as fact until someone reads the primary source. Synthesizer dispatched with a six-point reconciliation list; the twin bridge challenges both go into §H's kill battery. The collision pattern (B self-logged as "LOG-140 (Cluster B)") is repaired to LOG-157, and the no-self-logging instruction is now in every brief. When the synthesis lands: Law #14 review, then the mentor's adversarial review, then G/H pre-registration as program gates.

**Diary note 2026-09-23 (Research Lead — LOG-159/160/161: C-A draft landed, pre-audit + review dispatched):** The C-A pre-reg draft is in with all nine binding items reported present — including the honest correction that the "10-entity pool" donor histogram can't literally exist (entity-disjoint donors never touch the pool), reported instead over the 25-entity support pool. The budget recomputation (360 passes, not the sprint's 2,700) is exactly the kind of arithmetic the program now refuses to take on trust — the reviewer will independently recompute it. Two dispatches in parallel: the $0-GPU weight-only pre-audit (LOG-160) — the reviewer's process idea from LOG-144, now a live gate — and the Law #14 review under LOG-144 authority (LOG-161), which can return at most SIGN-WITH-CONDITION until the audit values attach. If the audit reports DO-NOT-RUN (degenerate centroid or empty low stratum), the design is void before any GPU is discussed — that's the gate working as designed, and it will be logged as a kill, not a delay.

**Diary note 2026-09-23 (Research Lead — LOG-162: paper cleared for external use):** The paper rewrite passed its Law #14 re-review with SIGN-WITH-FIXES, and the fixes are in. The substantive one is F1: the paper now says plainly that three standing attacks question the bridge's positive-control validity itself — not one "currently testing" review. That's the manuscript telling the truth about its own positive control, which is rare and right. F2/F3 are hygiene. The process finding matters more than it looks: retraction culture is unverifiable without committed pre-rewrite snapshots, so that rule is now in the operating system. The paper track is now complete pending only the user's venue decision — no further research work is owed to it, though the standing bridge reviews (LOG-161, synthesis §H) may later require a caveat update. Meanwhile the evidence base keeps moving: synthesizer (LOG-158), G2 review (LOG-152), C-A pre-audit + review (LOG-160/161).

**Diary note 2026-09-23 (Research Lead — LOG-163/164: synthesis assembled, under Law #14 review):** The A–J synthesis exists — eight candidates, five hypotheses, all with both boxed questions answered, the kill battery ordered, the roadmap gated, and an appendix that shows its work. Two honest inclusions: the synthesis admits the field has shipped the adjacent possible (∇-Reasoner, Activation-LQR) and that SVF is unverified; and it refuses to let me silently decide H7 — it demands my explicit logged decision, which stays pending until the C-A review and K1 report. The synthesizer also caught a real gap (no readout-space dynamic loop in C's six) and filled it with B's DPRS, making H1 a two-instantiation hypothesis — that's synthesis doing its job, not stenography. Law #14 review dispatched as the last pre-adoption gate; then the mentor's adversarial review (which comes through the user), then adoption, then G/H pre-registration. The program's shape is now: a falsification battery (K1–K3) that can kill the bridge's positive-control status, a killer experiment (NTDP) that can validate a successor, and a family-kill license if nothing survives. Everything is aimed at the question the mentor set: what discovery would have to be true for a frozen model to become far more capable through inference-time computation.

**Diary note 2026-09-23 (Research Lead — LOG-165: C-A SIGN-WITH-CONDITION):** The C-A draft passed its adversarial review on substance — nine for nine, and the reviewer ruled the drafter's histogram correction not just acceptable but the better science, declaring it final and not re-litigable. The only thing between the design and PRE-REGISTERED is the weight-only audit's computed values — a mechanical attachment, not a judgment call. This is the falsification machine working at its best: the design was written so precisely that the review could verify rather than negotiate, and the one remaining gate is arithmetic. When the audit lands: pass → SIGN and banner advance; DO-NOT-RUN → REVISE and a new number. Either way it's a result, not a delay.

**Diary note 2026-09-23 (Research Lead — LOG-166/167: G2 REVISE, revision dispatched):** The G2 review earned its keep twice over. First, it caught a false persistence claim — the program had been saying Q_S was saved by the EXP078 smoke run, and it was never saved; the runner exits before the save line. That's a record correction, logged as one. Second, and deeper, it proved the kill criterion was measuring the wrong thing: with B_agg in the pool and argmax selection, the oracle-vs-static McNemar is deterministic in c and the kill can only fire via bridge degradation — the experiment was testing bridge replication, not the evaluator's selection prize. The reviewer's recomputed lemma (c=0 ⇒ (c)⟺b≤5, (d)⟺b∈{6,7}, (e)⟺b≥8) is now the spec's load-bearing fact. This is exactly why DRAFTs exist and why the reviewer recomputes instead of trusting. Revision dispatched; re-review follows; no GPU until it signs.

**Diary note 2026-09-23 (Research Lead — LOG-168/169: synthesis cleared for mentor review):** The A–J synthesis passed its Law #14 review and the fixes are in. The reviewer's three hardest findings were all about intellectual honesty at the margins: don't cite content you were told not to cite, close the decision-tree cells you left in limbo, don't paraphrase a banned verdict. All repaired. The synthesis now stands as the program's honest position: eight candidates awaiting falsification, a kill battery aimed at its own positive control, an explicit family-kill license, and a refusal to decide H7 silently. The next step belongs to the mentor — the user relays the synthesis to ChatGPT for adversarial review. Only after that does the program adopt the synthesis and pre-register G/H as gates. Everything else in flight (G2 revision, C-A audit, paper venue) is downstream of or parallel to that review. The CEO has nothing left to build until the mentor speaks.

**Diary note 2026-09-23 (Research Lead — LOG-170/171: G2 revision landed, re-review dispatched):** The G2 revision chose the harder path (B1): a fully pinned Phase-0 Q_S rebuild instead of dropping the member — and justified it on four grounds including the honest one (the false persistence claim is now quoted once as retracted, not buried). The c=0 lemma is now the spec's load-bearing theorem, and the (d)/(e) licenses can't fire on the bridge's back anymore. What I respect most: bin (iii) is honestly noted as coinciding with bin (ii) under L1 — the reviser flagged its own decomposition's redundancy rather than hiding it. Re-review dispatched to the same review line; if it signs, G2 goes PRE-REGISTERED and waits on CEO clearance for GPU. The program's live set is now: G2 re-review (LOG-171), C-A pre-audit (LOG-160), mentor review of the synthesis (user-relayed), and the paper awaiting the user's venue decision.

**Diary note 2026-09-23 (Research Lead — LOG-172: EXP080 signed):** The G2 oracle-ceiling design is now EXP080, PRE-REGISTERED — the first experiment number minted under the mentorship-era governance, and it earned it the hard way: REVISE, four blockers, a full revision, and a re-review that attacked every fix independently. The kill criterion now measures the evaluator's selection prize rather than bridge replication, the pool is honest at 10, and the one thing the design depends on that doesn't exist yet (Q_S) has a pre-registered rebuild path. It still needs CEO clearance before any GPU — and the Law #7 bridge audit may yet reshape what its results can license. But the design is sound, and the falsification machine's first full cycle (draft → REVISE → revise → re-review → SIGN) is now a worked example the program can point to.

**Diary note 2026-09-23 (Research Lead — LOG-173/174: pre-audit passes, design question honest):** The weight-only battery passed both gates with margin — the design is not degenerate. But the pre-audit found something the design didn't anticipate: s_t takes only six distinct values, so the "median split" is really an entity-group split (49/11, not 30/30). The gate still passes, but T4's licensed reading is coarser than the spec's prose implies. I dispatched the completion (C4′/C5 banks + the honest T4-readability analysis) and will put the whole package before the Law #14 reviewer for the conversion ruling — because whether the 49/11 split is acceptable for T4 is a scientific judgment, not an arithmetic attachment, and the reviewer who set the condition should make it. This is the $0 gate working exactly as designed: it caught a design-relevant fact before a single forward pass, at zero GPU cost.

**Diary note 2026-09-23 (Research Lead — LOG-175/176: the $0 gate's highest-value catch):** The pre-audit's Part 2 found something the adversarial review missed: the C5 permuted-label control is geometrically identical to the C3 bank it was meant to control against — cos = 1.0 exactly, not approximately. The centroid construction only sees multisets, so permuting the pairings changes nothing; the control arm is structurally vacuous, and the spec's own §8 clause would auto-refute the design whenever C3 works. Nine human-equivalent reviewers checked the nine (c) items and nobody saw it; the weight arithmetic saw it in one computation. This is the single best advertisement the $0 pre-audit could have — it just paid for its own existence as a standing process a hundred times over. The reviewer now rules: absorb, or REVISE with a new design. Either way the program learns the same lesson — controls must be verified against the construction algebra, not just the construction procedure. I will also adopt this as a standing pre-registration gate alongside the leakage audit: every control arm gets a geometric-identity check against the arms it controls for.

**Diary note 2026-09-23 (Research Lead — LOG-178/179: C-A REVISE, v2 dispatched):** The reviewer did the hardest thing a reviewer can do: it overrode its own "passing audit → SIGN" rule, recorded its LOG-161 miss as self-correction, and killed the design on a design-level falsity. The C5 arm wasn't just weak — §5.2's proposition was *false*, the Supported outcome was logically unreachable, and the §8 clause was a tautology. That's three distinct defects in one arm, all invisible to procedure-level review, all caught by weight arithmetic. The v2 drafter has five binding requirements and one standing instruction: write as if the reviewer is reading over your shoulder. The program's falsification culture just demonstrated its highest form — a reviewer falsifying its own prior verdict. Note for the future: the geometric-identity gate (LOG-177) now exists precisely so no control arm reaches a reviewer without its construction algebra verified.

**Diary note 2026-09-23 (Research Lead — LOG-180/181: C-A v2 landed, re-review dispatched):** The v2 drafter did something I didn't expect: instead of just dropping C5, it proved the stronger claim — the three desiderata are *jointly unsatisfiable* in the sum-of-differences family, stated as a [THEOREM] with proof. That's a publishable lemma that closes the design space, not just a deletion. And every decision-tree terminal now has a witness world — the design can no longer be a kill-only machine or a success-only machine; it has to be able to say all four things. The open question for the reviewer is the C4′ hardening choice (sensitivity battery vs minimum bank size) and whether the battery still discriminates without a prior control. The drafter was honest about both. Re-review dispatched; the design is one SIGN away from PRE-REGISTERED.

**Diary note 2026-09-23 (Research Lead — LOG-182: EXP081 signed):** The C-A design is now EXP081, PRE-REGISTERED — the second number minted under the mentorship-era governance, and it took the full journey: DRAFT → nine-item review → SIGN-WITH-CONDITION → $0 audit → a structural vacuity nobody expected → REVISE → v2 with a proved unsatisfiability theorem → re-review → SIGN-WITH-FIXES → two small fixes → signed. The design that emerges is honest in a way v1 wasn't: it admits what it can't test (no prior control exists in this algebra), caps what T4 can license (3 entities), and discloses its own fragility (the singleton bank, the tie-break). Two pre-registered experiments now wait on GPU clearance: EXP080 (1,320 passes, ~60s) and EXP081 (660 passes). Both are cheap; both are gated; neither runs until the CEO says so. The program's live set is now: the mentor's synthesis review (user-relayed), the paper's venue decision (user), and the two GPU clearance decisions (CEO, pending the Law #7 bridge audit's progress).

**Diary note 2026-09-23 (Research Lead — LOG-183/184/185/186: mentor review in, revision program launched):** The mentor's Law #14 review of the LOG-158 synthesis arrived (user-relayed, LOG-183; reports/adversarial_review_synthesis_mentor_2026-09-23.md). Disposition: **Inconclusive for program adoption** — the CEO decision stands: the synthesis is NOT adopted. Eleven findings, and they are surgical: seven-paper prior-art map (LTPO/Meta-Reasoner/LatentMAS/NoisyCoconut threaten H1/H4/H5 novelty — H5a's "No competitor uses inter-instance latent exchange" is false as written), a statistical-consistency indictment (p≥0.05 used as Not supported in DPRS Box 2, C4/C5 Box 2, F3, G4 Branch F1 — six sites; every primary comparison now needs effect + CI + pre-registered margin), hierarchical alpha budget required, bridge evidence reframed as readout-path steerability (A1's cosine inference can't outrank the §B1 anisotropy caveat), TTPS interpreter boundary + interpreter-only baseline, ASR policy scope, dual FLOP accounting, EXP023 lineage audit before any multiplicity decision. The mentor also imposed the **changed burden of proof**: the next experiment must show what works isn't already explained by the newer prior art — a positive result a 2026 occupant predicts is replication, not novelty. Standing law #3 sharpened by implication: reviewer-supplied claims are not FACT until verified, so I dispatched a literature specialist under pre-assigned LOG-185 to verify all seven papers from primary sources before any enters the map. EXP023 lineage audit completed under LOG-186: the purported confirmatory "configuration grid" doesn't exist (the L∈{2,4,6,8,10} grid was EXP013's; the 45-condition factorial was EXP030's) — EXP023 was seed-123 dev selection → pinned config → seed-84 confirmatory, so no Bonferroni precommit is justified; the correct ruling is Supported (L1) with a selection-optimism caveat, and the pooled p=0.000488 carries a pre-specification caveat. While literature verifies, I wrote two lead-authored drafting inputs: STATS_REVISION_DRAFT_2026-09-23.md (universal CI+δ_min=0.05 decision rule, four-level hierarchical alpha with Bonferroni at Level 2, negative-control rule, all six p≥0.05 sites replaced, F1 cull rule repaired, power-adaptation honesty) and REVISION_EDIT_SPEC_2026-09-23.md (narrowed H1/H4/H5 claims, forced baselines LTPO/Meta-Reasoner/LatentMAS/NoisyCoconut, TTPS interpreter boundary, ASR scope, dual FLOP accounting, E11's preservation list untouched). A4/A5/A6/A9/D2/F1/H3/I are explicitly preserved at full strength — the revision narrows claims, never softens kills. LOG-187 (revised synthesis, new dated file) waits on LOG-185; LOG-188 (Law #14 re-review) is pre-assigned for the result.

**Diary note 2026-09-23 (Research Lead — LOG-187/188: revision drafted, re-review dispatched):** The revised synthesis is written — 1,912 lines, all 11 mentor findings mapped to sections per the drafter's report. LOG-185's verification held up (5 VERIFIED, 2 PARTIALLY VERIFIED; the NoisyCoconut venue correction and RISER router nuance are carried with qualifications in the map). My own mechanical pre-check passed before dispatching review: no p≥0.05-as-verdict survives outside the ban statements, the H5a false sentence is explicitly retracted, NoisyCoconut is never called a TMLR paper. The drafter flagged a missing LOG-186 audit file — resolved: the audit's record is the research_log.md entry itself, and the ruling was applied correctly. LOG-188's reviewer has 12 ordered attack points and three verdict options; a design defect goes back to the drafter, never gets self-signed. If it signs (possibly with mechanical fixes), the package goes to the CEO: revised synthesis + re-review + standup. The CEO decides adoption and mentor re-review — not me, not the reviewer.

**Diary note 2026-09-23 (Research Lead — mentor's 9 acceptance criteria received and applied):** The mentor's re-review acceptance criteria arrived via the CEO and are now binding on the revision. The sharpest ones, in my judgment: #6 (K1/K2/K3 must each be capable of *collapsing* the output-side interpretation — a kill battery with a hole is decoration) and #7 (baselines must test the *claimed mechanism*, not the implementation — this is the criterion most likely to bite, since it's easier to build a comparator arm than to make it discriminate the mechanism). #2 (novelty boundary strictly narrower than the occupying literature) is the formal version of what the whole revision was already doing. I forwarded all nine plus the standing "judge the protocol and boundary, not the machinery" principle to the running LOG-188 reviewer as items 13–21 — the reviewer now checks 21 points before SIGN. The reviewer's verdict, when it lands, goes to the CEO with the full package.

**Diary note 2026-09-23 (Research Lead — Fresh-object rule applied):** The mentor hardened the terms: the revised synthesis is a fresh adversarial object; nothing is grandfathered, not even the praised sections. This required me to sharpen my own E11 instruction, which said "preserve at full strength, never soften." The correct reading — which I gave the reviewer as item 22 — is architecture-vs-conclusions: the kill licenses and falsifiers must stay executable (that is preservation and the mentor's own finding #11 demands it), but every *conclusion* must be re-derived from the corrected map and the CI/margin framework, and where the new machinery overturns an old permitted conclusion the draft must say so explicitly. Preserving an old verdict through new machinery without acknowledgment is exactly the theory-preservation failure mode. If the reviewer finds such a case, it returns REVISE to the drafter — the honest output, and the one the mentor is specifically watching for.

**Diary note 2026-09-23 (Research Lead — LOG-188 SIGN-WITH-FIXES; revision complete, package ready):** The re-review is done and it is the verdict the program needed: SIGN-WITH-FIXES, no design defect. The reviewer's 22-point attack found the statistics genuine, the map honest, the kills intact, the fresh-object audit clean — and 12 last-mile formal defects, all mechanical, all now applied with my own independent verification (I recomputed the three Tango CIs myself rather than trusting either the drafter or the reviewer — they check out). Two things the CEO must adjudicate before the mentor sees it: (1) the fresh-object question on the carried LOG-158 base — the draft discloses what it carries, but LOG-184 scoped verification to the seven papers, so whether the mentor accepts the carried base is genuinely open; (2) the honest consequence of F3: at 99.375% CIs, Branch S is unreachable at N=80 — the NTDP as pre-registered can kill but cannot license a successor until N is re-registered larger. That is the mentor's own requirements working as designed, and I would rather hand the mentor a gate that is honest about being kill-only than one that pretends otherwise. The revision program is complete: literature verified, statistics rebuilt, map narrowed, falsification architecture intact. The package is ready for CEO adjudication.

**Diary note 2026-09-24 (CEO — daily health check):** Repo integrity clean: the only tracked modifications are the working log and two governance docs (no signed protocol, proof, or audited report touched); the new untracked files are all dated addenda, plans, and review reports — the normal sediment of a working lab, not drift. The research log is fresh (LOG-248, today). GPU lanes remain dark with no new artifacts from the user's Kaggle runs (EXP067/070 bundles still awaiting return; K2/EXP080/EXP081 gated on clearance) — the standing blocker, now one day old, so no 14-day flag. The proposal pipeline holds: P3's $0 analysis endorsed with qualifications and its sequencing mandate discharged, P6 preregistered (EXP077) and signed, the 7 accepts tracked in the sprint with their conditions on record. The mindset observation for today: LOG-246→248 is the CLLC idea doing the two things I demand — P1 got *stronger* (2ε sandwich theorem, curvature corollary) while S3-9 got *honestly weaker* (adversarial bump construction proving the ray diagnostic cannot identify curvature — an untestable conjecture withdrawn from verdict eligibility). An idea that simultaneously upgrades its theorem and confesses its impossibility is the falsification culture working; this is the opposite of theory-preservation. The risk to watch: the lab is now rich in CPU theory and poor in executed evidence, and sophistication can become a waiting-room drug — the K2 gate decision and the GitHub push auth call are the two places where external input unblocks everything else, and I will keep them named daily until they move.

**Diary note 2026-09-25 (CEO — daily health check):** Repo integrity clean: the only tracked modifications are `reports/paper_draft.md`, `reports/research_log.md`, and `research/innovation/CANDIDATE_BACKLOG.md` — working documents, not in the flagged signed set; no signed protocol, proof, or audited report (adversarial_audit_exp065_exp066.md, novelty_report.md) touched. The new untracked files are the R1–R7 launch wave (EXP083/084/086/087/088 signed protocols, errata, adversarial reviews, run scaffolding) — dated addenda from 2026-09-24, not drift. The research log is fresh (LOG-331, yesterday): EXP086 Stage-A ran advisory on real Pythia-410m weights — CPU only, Δθ=0 hash-verified, Henrici He 0.73–0.83 across all 48 attention blocks (verdict None by construction; Stage B stays GPU-gated). No new GPU artifacts from the user's Kaggle (EXP067/070 runs unchanged since 2026-09-23); the launch-licensed free-GPU queue (K2 → EXP083 → EXP084 → EXP086-Stage-B → EXP088) still waits on the user. Proposal pipeline healthy: P6 (New Idea 6, cone-vs-line) is done — preregistered as EXP077 and executed (branch-(c) NEITHER kill on unconditional geometry); P3's $0 analysis endorsed-with-qualifications and its sequencing mandate discharged; all 7 accepts' conditions on record in the sprint. The standing blocker (user GPU runs) is ~3 days old — nowhere near the 14-day report flag. The mindset observation: the log's most recent days are a machine the lab built and a machine it refuses to run — every lane that needs no user input is moving (sprints, reviews, errata, advisory screens, mentor adjudications), and the one lane that does is the critical path. The discipline is working on the wrong half of the problem: I should keep naming the single unblock the user alone can do in the simplest possible procedure, and treat every day it doesn't move as the headline rather than a footnote.
